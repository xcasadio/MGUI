# Taches systeme d'animation (V3)

## Objectif

Livrer la V3 du systeme d'animation de MGUI selon l'ADR-0008 (`Docs/decisions/0008-animation-v3-editor-readiness-and-remaining-limits.md`, statut Proposed tant que les dix questions ci-dessous ne sont pas tranchees), au-dessus de la V1 (ADR-0006) et de la V2 (ADR-0007, `Docs/Tasks/animation-v2-tasks.md`, `Docs/animation-architecture.md`) : rendre le moteur pret pour l'editeur de timeline du moteur de jeu de l'auteur (chemins animables par type d'element, `Seek`, clip de keyframes multi-pistes, serialisation d'un storyboard, easings de Bezier en texte), lever les deux limites V2 sur les pilotes (ecriture sous un run non vue avant la fin, valeur locale qui masque un etat nomme), donner au `MGToggleButton` un slot `Checked` sans toucher aux enums, migrer `MGTextBlock.TextCharactersPerSecond` et `MGHighlightBorderBrush` sur l'horloge du moteur, et, en dernier, le refresh a chaud des transitions et etats des styles.

Contraintes non negociables : celles de la V1 et de la V2 (pas de dependency property system, store ADR-0005 pour les pilotes, zero cout pour un element qui n'anime rien, tokens interdits de `RenderingBoundaryArchitectureTests`, renderer neutre, une ADR par decision) ; les enums `PrimaryVisualState` / `SecondaryVisualState`, `UIValueSlot` et `UIResolvedPropertyStore.SlotCount` restent inchanges ; l'ordre global de precedence ADR-0005 reste inchange (un nouveau palier 95 s'insere, rien ne bouge) ; aucune API publique V1 ou V2 n'est renommee ; les tests V1 et V2 (`MGUI.Tests/Animation/*`) restent verts sans modification sauf ajout ou re-ecriture explicitement listee dans une tache.

Hors programme (moteur de jeu de l'auteur) : widgets de timeline, scrubber, edition de courbes, undo / redo, fichiers projet, resolution des elements par nom. Differe (ADR-0008) : remap temporel d'un composite, presets CSS nommes (`ease`, `ease-in`...), effets de `MGTimer` sur le moteur, applicabilite par predicat.

## Historique du fichier

- 13 septembre 2026 : creation apres l'audit V3 en lecture seule a HEAD `e52880c` (trois lecteurs independants : styles et `Checked` ; extensions du moteur et limites pilotes ; consommateurs de temps et outillage editeur). Aucune tache commencee ; dix questions ouvertes a l'auteur.
- 13 septembre 2026 : reponses de l'auteur aux dix questions : toutes les recommandations en gras acceptees telles quelles (dont le palier 95 au-dessus de `LocalBinding`, U9 conservee en derniere tache, ordre U1-U5, U6-U8, U10, U9, U11) ; mode AUTO ; tranche U0 ajoutee avant U1 pour remettre la suite complete au vert (22 tests casses par les commits « clean code » du 13 septembre, HEAD `df22af7`). Les faits du chapitre « Contexte » ont ete re-verifies a `df22af7` par huit lecteurs independants : tout tient, seuls les numeros de ligne ont bouge (reformatage), `UIToolingService` est dans `MGUI.Core/Tooling/`, `MGToggleButton` dans `MGUI.Core/UI/`, `UIValuePrecedence` est un enum, `UIKeyFrameSerializer.Format` / `Parse` sont `private static`.

## Contexte : faits verifies le 13 septembre 2026 (HEAD `e52880c`, lecture seule)

- Easings : `UIEasing` (`MGUI.Core/UI/Animation/Easing/UIEasing.cs`) : dix-neuf `DelegateEasing` dans un registre insensible a la casse, `TryGet(name)` (`:99`) est une recherche de dictionnaire sans repli, `Register` (`:83`) est le point d'extension, `Names` (`:80`) enumere les cles ; `IUIEasingFunction.Ease(float)` sans contrainte de bornes (depassement autorise). Consommateurs, tous par nom : `Transition.Easing` (`XAML/Animation.cs:86`, `:108`), `MGThemeAnimationSettings.*Easing` (`MGTheme.cs:201-203`, `ThemeDefinitionBuilder.ParseAnimationEasing`, `UIThemeTransitions.Install:42`), `UIKeyFrameDto.Easing` (`KeyFrames/UIKeyFrameSerializer.cs:170`, resolu par `UIKeyFrame<T>.ResolveEasing`), `UIAnimationBuilder<T>.Ease(string)` (`UIAnimateExtensions.cs:163`). Aucun code de Bezier sous `Animation/` (`Graph/Rendering/GraphBezierGeometry.cs` echantillonne une courbe, il n'inverse pas x(t)).
- Composition : `UIAnimationGroup` (`Composition/UIAnimationGroup.cs`) : `TargetKey` synthetique (`:27`), `IsStoreBacked` faux (`:62`), `BaseValueBoxed` nul (`:66`), `ApplyProgress` (`:87`) protege par `_IsDriving` et ne fait que `StartDueChildren` ; chaque enfant est enregistre sur son proprietaire (`StartChild:131`) et avance par le manager avec son propre `Advance(delta)` incremental. `UIKeyFrameAnimation<T> : UIPropertyAnimation<T>` (`KeyFrames/UIKeyFrameAnimation.cs:16`, `OnStarting:31`, `ApplyProgress:60`) pilote un seul chemin ; `UIKeyFrameTrack<T>` ne connait pas de chemin ; `UIKeyFrameSerializer` serialise une piste par document (`Serialize<T>:31`, `Deserialize<T>:48`, `UIKeyFrameTrackDto:162 { Version, ValueType, Frames }`, `UIKeyFrameDto:170 { Offset, Value, Easing }`). Le sample construit a la main un `UIStoryboard` de trois enfants et une sequence avec un pop en keyframes (`MGUI.Samples/Features/AnimationDemo.xaml.cs:136-172`).
- Seek : `UIAnimation.Advance(TimeSpan delta)` est `internal` et incremental (`UIAnimation.cs:215`, `Elapsed += delta`), retour anticipe hors `Running` / `Delayed` (`:217-220`) ; `Begin` interne (`:188`) ; `UIAnimationCollection.Start` lie toujours au manager du desktop (`UIAnimationCollection.cs:19`) ; `InternalsVisibleTo` : `MGUI.Tests` et `CasaEngine.Tests` seulement (`MGUI.Core/Properties/AssemblyInfo.cs:6-7`). Une relecture image par image via `IUIDesktopRuntime.ApplyFrame` (precedent `MGUI.Tests/Tooling/StableDiagnosticIdTests.cs:362-402`) n'avance qu'en avant, rejoue tous les evenements et re-layoute a chaque image pour un pilote de layout.
- Applicabilite : `UIAnimationTargets.Paths` / `GetValueType` (`UIAnimationTargets.cs:38-51`) sans type proprietaire ; `IUIAnimationTarget<T>` : `Path`, `IsStoreBacked` ; contraintes par cast : `ForegroundTarget.Require` (`Targets/UIColorAnimationTargets.cs:142-144`), `ProgressButtonValueTarget.Require` (`Targets/UIBuiltInAnimationTargets.cs:178`) ; environ vingt-et-une classes de cibles (onze, quatre, six).
- Store et transitions : `GetUnderlyingValue => GetValue` sur les quatre cibles couleur (`UIColorAnimationTargets.cs:94, 122, 157, 195`) ; `UITransition<T>.HandleChanged` (`UITransition.cs:170-177`) lit cette valeur physique ; `UIResolvedPropertyStore.Set<T>` (`Styling/UIResolvedPropertyStore.cs:141`, insertion triee `:161`), `TryGetWinner<T>` (`:232`) rend la premiere contribution ; `EnumerateResolvedContributions` (`MGElement.cs:1526-1538`) sert deja de contournement dans `UIStoreBackedTargets.Restore` (`IUIStoreBackedAnimationTarget.cs:51-78`, branche `{Animation}` seul `:74-78`) et `UIVisualStateApplier<T>.CaptureBase` lit `UITransition<T>.SettledValue` (`States/UIVisualState.cs:200-206`) ; `UIPropertyAnimation<T>.ReadCurrentValue` (`UIPropertyAnimation.cs:39`) doit continuer a lire la valeur animee (ADR-0006 : une animation explicite part de la valeur courante). Cas speciaux de dormance `Background` / `DefaultTextForeground` dans `TryGetResolvedPilotValue<T>` (`MGElement.cs:1353-1380`).
- Precedence (`Styling/UIValuePrecedence.cs:11-15`) : `Template` 60, `VisualState` 70, `LocalBinding` 80, `LocalValue` 90, `Animation` 100 ; le setter d'etat ecrit a 70 (`UIVisualState.cs:187`) ; `VisualStatesTests.ALocalValue_ShadowsANamedStateOnAPilot` (`:196-215`) verrouille la limite.
- `Checked` : `PrimaryVisualState` a quatre membres (`VisualState.cs:17-27`), `VisualStateSetting<T>.GetValue(PrimaryVisualState)` (`:129-137`) est le resolveur unique (`GetUnderlay:296`, neuf sites : `MGElement.cs:3010`, `:4310`, `:4318`, `MGGridSplitter.cs:568`, `MGUniformGrid.cs:993`, `MGProgressBar.cs:447`, `:454`, `MGResizeGrip.cs:282`, `UISymbolElements.cs:280`) ; `ResolvePrimaryVisualState` (`MGElement.cs:2898-2916`) ; DTO de theme a quatre valeurs (`XAML/Themes.cs:133-161`, `ThemeDefinitionBuilder.cs:791-835`) ; `UIValueSlot` a six membres et `SlotCount = 6` (`UIResolvedPropertyStore.cs:29-30`, `:91`) ; seul `MGToggleButton.IsChecked` ecrit `IsSelected` (`MGToggleButton.cs:93`), `CheckedBackgroundBrush` / `CheckedTextForeground` sont des alias documentes de `SelectedValue` (`:46-70`) ; `MGCheckBox` (`MGCheckBox.cs:41, 96-98, 224-226`) et `MGRadioButton` (`MGRadioButton.cs:139, 249-251, 294-296`) dessinent leur etat coche par des elements enfants ; `FocusTests.cs:196-199, 392-404` enumerent les quatre membres.
- Refresh de styles : `ElementStyleScope` (`XAML/ElementStyleScope.cs:9-44`) capture une fois `DefinitionType`, `ElementType`, `StyleNames`, `IsStyleable`, `UsesResourceStyles`, `InlineStyles`, `StyledPropertyNames`, rien sur les transitions ni les etats ; `ElementStyleRefresher.RefreshElement` (`:176-288`) ne lit que des `Setter` (`CollectSetters:290-299`, `FindNamedStyle:302-313`, `Pass.GetImplicitStyles:144-153`), onze `RefreshableProperty` (`:51-90`), le reste en `Skipped` / `NotRefreshable` (`Styling/UIStyleRefreshResult.cs:7-53`) ; transfert unique dans `Element.ApplyBaseSettings` (`Element.cs:619-645`, styles puis element, remplacement par chemin ou nom par `UITransitionCollection.Add:31-51` et `UIVisualStateCollection.Add:35-55`) ; `CollectStyleAnimation` (`Element.cs:1234-1245`) ; `MGResources.AddImplicitStyle` fusionne par chemin et nom (`MGResources.cs:559-584`) ; `OnStyleAdded` / `OnStyleRemoved` (`:629, 637, 646-647`) sans abonne ; `RefreshedStyleProperties` (`MGElement.cs:1556`) ; suite `MGUI.Tests/Architecture/StyleRefreshTests.cs`.
- Consommateurs de temps : `MGTextBlock.UpdateSelf` (`MGTextBlock.cs:1398-1404`) cumule `TextProgress` (`double?`), remis a zero par le constructeur (`:1237`) et le setter de `TextCharactersPerSecond` (`:711`) seulement, pas par un changement de `Text` ; `RemainingCharacters` au dessin (`:1420`). `MGHighlightBorderBrush.Update` (`Brushes/Border Brushes/MGHighlightBorderBrush.cs:504`) cumule `UA.FrameElapsed / CycleDuration`, brush partagee et dedupliquee par frame (`:481-483`), `Target` optionnel (`:399-457`) : pas un element, pas de cle (proprietaire, chemin). Chronometres a garder : `MGTimer` (`:272-273` Flicker sur `Opacity`, `:332` compte a rebours avec son `TimeScale`, `:339-341` Shake), `MGStopWatch` (`:237`), delai de tooltip (`MGElement.cs:3727-3746`, horloge murale), clignotement du caret (`Text/MGTextCaret.cs:370-371`), repetition de `MGButton` (`MGButton.cs:318-345`). Sans consommation de temps : `MGProgressBar`, `MGScrollViewer` (`:705-724`), `MGSpoiler`, `Docking/Controls/MGDockAutoHideDrawer.cs`, `Graph/Interaction/GraphViewportTransform.cs`. `UpdateBaseArgs` (`MGUI.Shared/Rendering/RenderLoopArgs.cs`) est deja etendu par `with { }` dans `MGDesktop.Update` (`MGDesktop.cs:1400`, `PaintRegistry`) apres `Animations.Update` (`:1377`).
- Outillage : `UIToolingService.CaptureElementDebugView` (`Tooling/UIToolingService.cs:331`, `CaptureAnimations:429-448`) -> `UIElementDebugView.Animations` (`UIAnimationDebugView(Kind, Path, State, Progress, Name)`) et `VisualStateName` ; `MGDesktop.Animations` : `Clock` (`Time`, `DeltaTime`, `TimeScale`, `IsPaused`), `ActiveCount`, `ActiveAnimations` (liste vivante) ; aucun DTO pour `UIStoryboard`, `UISequenceAnimation`, `UIDelayAnimation`, `UIPropertyAnimation<T>`, `UIVisualState` ; aucun DTO XAML `Storyboard`.

## Questions a l'auteur (a trancher avant U1 ; recommandation en gras)

1. `Checked` : (a) membre `PrimaryVisualState.Checked` avec un slot dans les conteneurs, le store (`SlotCount` 7), les DTO de theme et de XAML, le refresher (douze fichiers et plusieurs tests epingles, risque de fond vide pour toute case ou radio dont `CheckedValue` n'est pas pose) ; (b) **garder `IUICheckable` et l'etat nomme V2, ajouter `VisualStateBrush<T>.CheckedValue` (fill et couleur seulement) consomme par `MGToggleButton` avec repli sur `SelectedValue` ; `MGCheckBox` et `MGRadioButton` inchanges ; propriete code seulement, pas de champ DTO de theme tant qu'aucune application ne le demande.**
2. Valeur locale contre etat nomme : (a) garder la limite ; (b) inverser globalement `VisualState` et `LocalValue` (rejete : casse l'ordre ADR-0005 pour tous les pilotes) ; (c) **`UIVisualState.OverridesLocalValue` (defaut faux), ecrit au palier `VisualStateOverride = 95` avec `Kind` inchange, expose en XAML sur `VisualStateDefinition` ; granularite par etat, pas par setter.** A confirmer : ce palier passe aussi au-dessus de `LocalBinding` (80).
3. Bezier : **syntaxe canonique `cubic-bezier(x1,y1,x2,y2)`, `bezier:x1,y1,x2,y2` accepte en entree, `ToString` en forme CSS ; `X1` / `Y1` / `X2` / `Y2` publics en lecture (relecture par un editeur de courbes) ; pas de presets CSS nommes (`ease`, `ease-in`...) ; mise en cache par litteral dans le registre (un editeur qui edite en direct doit quantifier ses points).**
4. Clip de keyframes : **un element par clip (l'appelant fournit l'element, comme `UIKeyFrameSerializer.Deserialize<T>` aujourd'hui) ; `Duration` du clip faisant autorite sur les pistes ; `Format` / `Parse` de `UIKeyFrameSerializer` passes `internal static` et partages ; remap temporel d'un composite differe explicitement.**
5. `Seek` : (a) `InternalsVisibleTo` vers l'assembly du moteur de jeu ; (b) **`UIAnimation.Seek(TimeSpan)` public, sans evenement, avant et arriere, autorise sur un composite, reserve aux instances de preview conduites par l'hote (jamais sur une animation enregistree au manager vivant) ; passe de conception sur `Completed -> Running` avant implementation.**
6. Serialisation d'un storyboard : **resolution des elements par delegue `Func<string, MGElement>` fourni par l'hote ; meme ensemble ferme de types de valeur que le serialiseur de keyframes, une cible applicative doit enregistrer une paire `Format` / `Parse` (point d'extension documente, miroir de `UIInterpolators.Register<T>`) ; dossier `KeyFrames/` a cote du serialiseur existant, DTO versionnes sans instance de controle (precedent `GraphSerializer`).**
7. Refresh a chaud des transitions et etats : (a) le retrait d'une transition ou d'un etat qui n'est plus style **restaure immediatement** (`Transitions.Remove` garde la valeur courante, `VisualStates.Remove` restaure) ; (b) **provenance par `UIValueSourceKind` (`ImplicitStyle` / `ExplicitStyle`)** plutot qu'un booleen ; (c) **tout chemin enregistre** (pas seulement les onze proprietes rafraichissables des setters) ; (d) **derniere tache du programme**, la seule de taille L cote styles, a confirmer qu'elle reste voulue.
8. Migrations : **`MGTextBlock.TextCharactersPerSecond` sur le moteur (contrat public en caracteres par seconde conserve, remise a zero de la progression sur un changement de `Text` : changement de comportement a annoncer), `MGHighlightBorderBrush` sur l'horloge du desktop par `UpdateBaseArgs.AnimationDeltaTime` (source de temps seulement, pas de propriete) ; `MGTimer` (Flicker, Shake, compte a rebours), `MGStopWatch`, delai de tooltip, caret, bouton a repetition : gardes tels quels.** Consequence a accepter : pause et `TimeScale` de `MGDesktop.Animations.Clock` s'appliquent au texte revele et aux brushes de surbrillance.
9. Applicabilite : **un `Type? RequiredOwnerType` unique (membre d'interface par defaut nul), pas de predicat ; toutes les contraintes actuelles sont des casts vers une classe concrete.**
10. Ordre : **U1 a U5 (gains rapides et limites pilotes), puis U6 a U8 (preparation editeur), puis U10 (migrations), U9 (refresh a chaud) et U11 (sample et validation).** Alternative : U6 a U8 d'abord si l'editeur est la prochaine etape immediate.

## Decisions de conception derivees (session principale, contestables avant U1)

- Bezier : `UICubicBezierEasing : IUIEasingFunction` (`Easing/UICubicBezierEasing.cs`, algorithme UnitBezier : Newton sur x(u) - t avec repli par bissection quand la derivee est proche de zero, `X1` / `X2` bornes a [0,1] a la construction, `ArgumentOutOfRangeException` sinon) ; `UICubicBezierEasing.TryParse(string)` ; `UIEasing.TryGet` : recherche par nom puis `TryParse`, l'instance construite est inseree dans le registre sous le litteral exact ; `Names` continue d'enumerer les cles (les dix-neuf built-ins restent un sous-ensemble, `EasingTests.Names_ListsTheNineteenBuiltIns` inchange).
- Applicabilite : `IUIAnimationTarget<T>.RequiredOwnerType` (`Type?`, membre d'interface par defaut => null) ; `UIAnimationTargets.GetOwnerType(path)` (null pour un chemin inconnu, comme `GetValueType`) ; renseigne par `ForegroundTarget` (`MGTextBlock`), `ProgressButtonValueTarget` (`MGProgressButton`) et toute cible qui caste.
- Store : `UIResolvedPropertyStore.TryGetWinnerExcluding<T>(property, slot, UIValueSourceKind excluded, out UIResolvedValue<T>)` (balayage lineaire de la liste triee, onze contributions au plus) ; `MGElement.TryGetResolvedPilotValueExcluding<T>` interne avec les memes branches de dormance ; `IUIStoreBackedAnimationTarget<T>.TryGetValueBelowAnimation(element, out T)` implemente par les neuf cibles store ; `UITransition<T>.HandleChanged` l'utilise pour une cible store, `GetUnderlyingValue` reste pour une cible simple ; ensuite `UIVisualStateApplier<T>.CaptureBase` perd son cas special et la branche `{Animation}` de `UIStoreBackedTargets.Restore` fusionne avec la branche « plus rien » ; les tests de `VisualStatesTests` qui decrivent la limite sont re-ecrits, pas supprimes.
- Etat contre local : `UIValuePrecedence.VisualStateOverride = 95` ; `UIVisualStateApplier<T>` construit `_LastSource` avec ce palier quand `UIVisualState.OverridesLocalValue` est vrai, `Apply` et `Restore` inchanges par ailleurs ; verification prealable par `rg` qu'aucun `switch` exhaustif ne porte sur `UIValuePrecedence` (seuls `Kind` sont enumeres) ; `VisualStateDefinition.OverridesLocalValue` en XAML.
- `Checked` : `VisualStateBrush<T>.CheckedValue` (`T`, defaut null, copie par les constructeurs d'heritage) et `GetValue(VisualState state, bool isChecked)` ; `MGToggleButton.CheckedBackgroundBrush` / `CheckedTextForeground` lisent `CheckedValue ?? SelectedValue` et ecrivent `CheckedValue` ; `IsChecked` continue de poser `IsSelected` (`MGToggleButton.cs:93`) ; le chemin generique `GetUnderlay` / `GetValue(PrimaryVisualState)` et ses neuf sites ne changent pas ; limite documentee : `Background.Checked` n'est ni une cible ni un slot du store.
- Clip : `KeyFrames/UIKeyFrameClipSerializer.cs` : `UIKeyFrameClipDto { Version, Duration, Tracks[] { Property, ValueType, Frames[] } }` ; `Deserialize(json)` resout le type de valeur par `UIAnimationTargets.GetValueType(path)`, construit `UIKeyFrameAnimation<>` par `MakeGenericType` (precedent `UITransition.Create`, `UITransition.cs:55`), pose la piste et la duree partagee, rend un `UIStoryboard` ; `Serialize(storyboard)` pour un storyboard ne contenant que des animations keyframes ; refus explicite : chemin inconnu, type inconnu, version inconnue, enfant qui n'est pas une animation keyframes.
- `Seek` : les maths de progression et d'iteration de `Advance` (`UIAnimation.cs:236-274`) extraites dans un helper prive partage ; `Seek(TimeSpan elapsed)` public : pose `Elapsed`, un seul `ApplyProgress`, aucun evenement, aucun `Manager.NotifyFinished`, aucun changement d'etat vers `Completed` (une preview qui atteint la fin reste `Running` jusqu'a `Cancel`) ; `UIAnimationGroup.Seek` positionne chaque enfant a `elapsed - offset` (clamp) ; refuse (`InvalidOperationException`) si `Manager != null` (animation enregistree) ; `Begin` d'une instance de preview par une surcharge interne exposee par une fabrique publique `UIAnimationPreview.Attach(element, animation)` (sans manager, `InheritsBaseValue` faux) ; le sweep du manager et `FinishedChildren` des groupes ne voient jamais une instance de preview.
- Storyboard : `KeyFrames/UIAnimationSerializer.cs` : `UIAnimationNodeDto { Kind (Storyboard | Sequence | Delay | Property | KeyFrames), Element (nom ou vide = racine), Path, ValueType, Duration, Delay, RepeatCount, RepeatForever, AutoReverse, FillBehavior, CancelBehavior, Name, From, To, Easing, Track, Children[] }`, `Version` 1 ; `Serialize(UIAnimation, Func<MGElement, string> nameOf)` ; `Deserialize(json, Func<string, MGElement> resolveElement)` ; erreurs explicites listant les chemins connus.
- Refresh a chaud : `UITransition.Provenance` et `UIVisualState.Provenance` (`UIValueSourceKind?`, internes, poses par `Transition.ToTransition` / `VisualStateDefinition.ToVisualState` quand ils viennent d'un style) ; `ElementStyleScope.OwnTransitionPaths` / `OwnVisualStateNames` captures dans `ProcessStyles` ; `ElementStyleRefresher.RefreshElement` : construit la liste des transitions et etats des styles courants (implicites puis nommes, meme ordre que `CollectStyleAnimation`), ajoute ou remplace ce que l'element ne declare pas lui-meme, retire ce qui etait style et ne l'est plus si l'entree courante porte encore la provenance ; `MGElement.RefreshedStyleTransitionPaths` / `RefreshedStyleVisualStateNames` ; `UIStyleRefreshResult.WrittenTransitions`, `ClearedTransitions`, `WrittenVisualStates`, `ClearedVisualStates` ; deux refreshs sans changement n'ecrivent rien.
- Migrations : cible `TextBlock.TextProgress` (`double`, simple, non observable, `MGTextBlock` seulement, ecrite par `ApplyAnimatedTextProgress`) ; `SyncTextProgressAnimation` depuis les setters de `Text` et `TextCharactersPerSecond` (part restante `(1 - progress) * NumCharacters / TextCharactersPerSecond`, `HoldEnd`, `KeepCurrent`, sous-classe privee neutralisant la restauration forcee comme `MGProgressButton.DurationRun`, rien hors de l'arbre) ; `UpdateBaseArgs.AnimationDeltaTime` (`TimeSpan`, delta deja mis a l'echelle, zero en pause) pose par `MGDesktop.Update` apres `Animations.Update` ; `MGHighlightBorderBrush.Update` cumule `UA.AnimationDeltaTime`.

## Consignes de travail pour l'agent IA

- Executer les tranches dans l'ordre ; une tranche = un commit ; mettre a jour le statut dans ce fichier dans le meme commit.
- Blocage : marquer ⛔, decrire, s'arreter.
- Pas de refactor hors perimetre ; aucun renommage d'API publique V1 ou V2.
- Tests sur le comportement observable avec le harnais headless (`AnimationTestScene`, frames explicites) ; zero allocation par tick pour un run en cours ; aucun slot pour un element sans animation ; `Seek` sans allocation.
- Chaque tranche modifiant `MGElement`, `VisualState.cs`, `MGDesktop`, le store, `UITransition`, `UIAnimation`, `Element.cs`, `ElementStyleRefresher.cs`, `MGTheme`, `UpdateBaseArgs` ou un controle est a risque : verification independante en lecture seule obligatoire (modele Sonnet), constats consignes dans le statut de la tranche.
- Toute decision prise en cours de tranche est ajoutee a ADR-0008 au moment ou elle est prise.
- Ne jamais lancer `MGUI.Samples` depuis un agent ; le construire a chaque tranche.
- Docs en francais sans accents.

## Taches

### ✅ U0. Remise au vert de la suite complete

**Statut** : livre le 13 septembre 2026. Les 22 tests en echec a `df22af7` ont ete realignes sur le source apres les cinq commits « clean code » (44c92b4, 8661fa7, 5b9fc7e, 61fc475, 655f36e), sans toucher a `MGUI.Core` et sans affaiblir, supprimer ni desactiver une assertion : famille A (7 tests, extraits `Type x = ...` devenus `var x = ...` ou champ prive renomme, `FocusArchitectureTests`, `ControlTemplateInfrastructureTests`) ; famille B (2 tests, dossier `Fill Brushes` renomme `FillBrushes`, `BrushRenderContextTests`, `RenderContextTests`) ; famille C (5 tests, champs prives de `MGElement` lus par reflexion renommes en camelCase : `_renderTransform`, `_layoutBounds`, `_animationSlot`, `_appliedTemplateDefaults` via `CreateOwnerStub`) ; famille D (2 tests, `ResolvedPilotWriteSitesTests` : lignes de `AllowedLines` et `PendingMigration` decalees, chemins de brushes). Suite complete : 0 echec sur 2017. Revue independante (verifier) : CONFIRMED, un constat P4 consultatif (certains extraits `var` correspondent desormais a plusieurs lignes du meme fichier ; l'invariant garde reste asserte par l'extrait compagnon).

Commit : `test: realign source-reading tests with the clean code reformat`

### ✅ U1. Easings de Bezier

**Statut** : livre le 13 septembre 2026. `Easing/UICubicBezierEasing.cs` (nouveau) : quatre flottants publics `X1`/`Y1`/`X2`/`Y2`, `X1` et `X2` valides a la construction dans [0, 1] (`ArgumentOutOfRangeException` sinon, comme toute valeur non finie), `Y1`/`Y2` libres (depassement autorise) ; `Ease` par Newton-Raphson (8 iterations, tolerance 1e-6) avec repli par bissection (32 iterations) quand la derivee est proche de zero, sans allocation ; `ToString()` en forme CSS canonique invariante ; `TryParse` / `Parse` acceptent `cubic-bezier(x1,y1,x2,y2)` et `bezier:x1,y1,x2,y2` avec espaces toleres, refusent le malforme et le hors bornes sans jamais lever (sauf `Parse`, qui leve `FormatException`). `UIEasing.TryGet` : recherche par nom inchangee, repli sur `UICubicBezierEasing.TryParse` sur un echec, insertion dans le registre sous le litteral exact (apres `Trim`), pas sa forme canonique (deux lookups du meme litteral rendent la meme instance, mais `cubic-bezier(0.42,0,1,1)` et `cubic-bezier(0.42, 0, 1, 1)` restent deux entrees). Aucun changement aux DTO XAML, au constructeur de theme, au serialiseur de keyframes ni a l'API fluente : ils acceptent le litteral sans modification, verifie par un test dans chacun des quatre points d'entree. `EasingTests.Names_ListsTheNineteenBuiltIns` tolerait deja un sous-ensemble (`BuiltInNames.All(UIEasing.Names.Contains)`) : aucune re-ecriture necessaire (l'ecart envisage a la question 3 ne s'est pas materialise). Tests ajoutes : `EasingTests` (26 methodes : valeurs des quatre presets CSS aux bornes et a mi-course contre un oracle par echantillonnage dense 100 000 pas, convergence pres des bornes et pour `X1 == X2` et une courbe raide, monotonie de `ease-in` sur 1000 echantillons, validation du constructeur, `TryParse` deux formes/casse/espaces/exposant/hors bornes/malforme, `TryGet` resout-cache-liste, `Parse` leve `FormatException`, zero allocation) ; `KeyFrameTests.Serializer_RoundTrips_ABezierEasingLiteral` (round-trip JSON, litteral conserve, `ResolveEasing` rend un `UICubicBezierEasing`) ; `XamlAnimationTests.Transition_AcceptsABezierEasingLiteral` (transition XAML) ; `StyleAndThemeAnimationTests.AThemeDefinition_AcceptsABezierEasingLiteral_ForHoverEasing` (theme). Verification manuelle hors suite : l'API fluente `Animate(...).Ease("cubic-bezier(...)")` accepte le litteral (sonde temporaire lancee puis retiree, aucun residu dans `FluentApiTests.cs`). Build `MGUI.Core` et `MGUI.Samples` : ok. Suite complete : 2066/2066 (0 echec). Revue independante (verifier) : un premier tour REFUTED (`TryParse` levait `ArgumentOutOfRangeException` sur un `y` non fini comme `1e40` au lieu de rendre faux), corrige (rejet des `y` non finis avant construction, 3 tests ajoutes), second tour CONFIRMED avec quatre constats P4 consultatifs : `0,42` en decimale a virgule peut donner une autre courbe valide quand il reste exactement quatre nombres (ambiguite inherente a la grammaire CSS) ; `TryParse` tolere un espace avant la parenthese et un `+` initial ; un litteral avec espaces exterieurs re-parse a chaque `TryGet` (cache sous la cle rognee seulement) ; compte de tests du statut corrige.

But : `UICubicBezierEasing`, forme textuelle, repli de `UIEasing.TryGet`.

Etat actuel : voir « Easings ».

Travail attendu :

- `Easing/UICubicBezierEasing.cs` : quatre flottants publics, `Ease` (Newton puis bissection), `TryParse` (`cubic-bezier(...)` et `bezier:...`), `ToString` en forme CSS, validation a la construction.
- `UIEasing.TryGet` : repli sur `TryParse` puis mise en cache sous le litteral ; `Names` inchange dans son contrat.
- Docs : section « Easings » de `Docs/animation-architecture.md` (syntaxe, cache, limite du cache pour une edition en direct).
- Tests `MGUI.Tests/Animation/EasingTests.cs` (extension) : valeurs connues des presets CSS (`ease`, `ease-in`, `ease-in-out`, `ease-out`) aux bornes et a mi-course, convergence pres des bornes et `X1 == X2`, `TryParse` accepte les deux formes et refuse le malforme et le hors bornes, `TryGet` resout, met en cache (meme instance) et expose le litteral dans `Names` ; `KeyFrameTests` : round-trip JSON avec un easing de Bezier ; `XamlAnimationTests` et `StyleAndThemeAnimationTests` : `Easing="cubic-bezier(...)"` et `HoverEasing` de theme acceptes.

Commit recommande : `animation: add cubic Bezier easings`

### ✅ U2. Applicabilite des cibles

But : `IUIAnimationTarget<T>.RequiredOwnerType`, `UIAnimationTargets.GetOwnerType`.

Etat actuel : voir « Applicabilite ».

Travail attendu :

- `IUIAnimationTarget.cs` : membre d'interface par defaut `Type? RequiredOwnerType => null` ; `UIAnimationTargets.GetOwnerType(path)` ; les cibles qui castent renseignent leur type (`Foreground`, `ProgressButton.Value`, et toute autre trouvee par `rg "element as MG"` dans `Targets/`).
- `UIToolingService` : la debug view d'un element peut lister les chemins applicables (`ApplicablePaths`, optionnel, sans allocation par frame : calcule a la capture).
- Docs : table des cibles avec une colonne « Element requis ».
- Tests `MGUI.Tests/Animation/TargetApplicabilityTests.cs` : chaque chemin framework rend le type attendu ou null, chemin inconnu rend null sans exception, un filtre `Paths.Where(GetOwnerType is null or assignable)` sur un `MGButton` exclut `Foreground` et `ProgressButton.Value`.

**Statut** (13 septembre 2026, livre) : `Type RequiredOwnerType => null` ajoute a `IUIAnimationTarget<T>` (membre d'interface par defaut) ; le registre `UIAnimationTargets` garde desormais un `Entry(Target, ValueType, OwnerType)` renseigne a l'enregistrement (`Register<T>` lit `target.RequiredOwnerType`), plus de reflexion dans `GetValueType` ; `GetOwnerType(path)` (null pour chemin inconnu ou element libre) et `IsApplicable(path, element)` ajoutes, memes garanties que `GetValueType` (insensible a la casse, jamais d'exception, chemin/element null accepte). Deux cibles seulement renseignent un type : `ForegroundTarget` (`MGTextBlock`) et `ProgressButtonValueTarget` (`MGProgressButton`) — celles dont le `Require` interne caste reellement. `BorderBrushTarget` reste a `null` : son `RequireBorder` appelle `element.GetBorder()`, une facade virtuelle que la plupart des elements a bordure (bouton, fenetre, ListBox, ComboBox...) redefinissent pour renvoyer leur propre `MGBorder` interne — ce n'est pas un cast strict vers `MGBorder`, donc n'importe quel element possedant une bordure accepte ce chemin, conformement a la consigne du brief. `UIElementDebugView.ApplicablePaths` est une propriete `init` (pas un parametre positionnel du record, pour ne rien casser d'existant) calculee uniquement dans `CaptureElementDebugView`, triee ordinal. Nuance de test : d'autres classes de tests (`AnimationManagerTests`, `TransitionTests`) enregistrent des cibles jetables sous un prefixe `Test.` directement dans le registre statique partage, qui ne les retire jamais ; `TargetApplicabilityTests` filtre ce prefixe pour ne comparer que l'ensemble ferme des cibles framework. Suite complete 0 echec (2099 tests). Revue independante (verifier) : CONFIRMED, trois constats P4 consultatifs : `ApplicablePaths` entre dans l'egalite synthetisee du record `UIElementDebugView` (tableau compare par reference, aucun consommateur ne compare deux captures) ; `IsApplicable` rend faux pour un element nul (choix defensif non documente dans le resume XML) ; le filtre `Test.` de la garde de couverture laisserait passer une future cible framework prefixee ainsi.

Commit recommande : `animation: expose the owner type of the animation targets`

### ✅ U3. Valeur sous l'animation pour les pilotes

But : `TryGetWinnerExcluding`, `TryGetValueBelowAnimation`, retarget immediat d'une transition sur un pilote.

Etat actuel : voir « Store et transitions ».

Travail attendu :

- `UIResolvedPropertyStore.TryGetWinnerExcluding<T>` (+ test unitaire a cote de `Set` / `Unset` / `TryGetWinner`) ; `MGElement.TryGetResolvedPilotValueExcluding<T>` interne (memes branches de dormance que `TryGetResolvedPilotValue<T>`, tests dedies pour `Background` et `DefaultTextForeground`).
- `IUIStoreBackedAnimationTarget<T>.TryGetValueBelowAnimation` sur les neuf cibles store (quatre couleurs, `Margin`, `Padding`, `MinHeight`, deux gradients).
- `UITransition<T>.HandleChanged` : valeur sous `Animation` pour une cible store ; `UIPropertyAnimation<T>.ReadCurrentValue` inchange (test de non-regression : une animation explicite demarree pendant le run part de la valeur animee).
- Simplifications : `UIVisualStateApplier<T>.CaptureBase` sans cas special de transition ; `UIStoreBackedTargets.Restore` sans branche `{Animation}` ; `VisualStatesTests.LeavingAStateMidTransitionOnAThemeBackground_ComesBackToTheThemeColour` re-ecrit (le retour commence a la sortie, plus a la fin du run).
- Docs : « Transitions », « Etats visuels nommes », « Limites connues » (la limite « vue a la fin du run » disparait pour les transitions).
- Tests `MGUI.Tests/Animation/TransitionTests.cs` (extension) : ecriture locale du fond pendant un run de transition couleur re-ciblee a la frame suivante ; sortie d'etat nomme pendant un run vue immediatement.

**Statut** (13 septembre 2026, livre) : `UIResolvedPropertyStore.TryGetWinnerExcluding<T>` ajoute (scan lineaire de la liste triee par precedence, premier gagnant dont le genre differe de celui exclu) avec ses quatre tests dedies (gagnant exclu -> le suivant remonte ; genre exclu non gagnant -> le gagnant reel remonte inchange ; seule contribution exclue -> faux ; entree vide -> faux). `MGElement.TryGetResolvedPilotValue<T>` refactore en un coeur prive `TryGetResolvedPilotValueCore<T>(property, slot, UIValueSourceKind? excluded, out UIResolvedValue<T>)` partage par la methode publique existante (`excluded: null`) et par le nouveau `TryGetResolvedPilotValueExcluding<T>` (rend `T` nu, pas `UIResolvedValue<T>` : c'est directement ce que consomme `TryGetValueBelowAnimation`) ; les deux aides de sous-slot (Background, DefaultTextForeground) recoivent le meme parametre optionnel et un petit relais prive `TryGetStoreWinner<T>` centralise le choix `TryGetWinner` / `TryGetWinnerExcluding`, evitant tout `out var` conditionnel mal defini pour l'analyse d'assignation definie du compilateur. Nouveaux tests dedies `MGUI.Tests/Animation/PilotValueBelowAnimationTests.cs` : Background et DefaultTextForeground avec une contribution `Animation` au-dessus d'une `LocalValue` (la lecture excluante rend la valeur locale) et les deux scenarios de dormance existants (sous-slot dormant puis resurfacant) verifies identiques entre `TryGetResolvedPilotValue` et `TryGetResolvedPilotValueExcluding` en l'absence de toute contribution `Animation`. Revue independante (verifier) : premier tour REFUTED (P2 : entree dans un etat nomme pendant un run declenche par un swap de conteneur entier, seule contribution `Animation` sur le sous-slot, le repli physique R6 attribuait la valeur en vol au gagnant `Whole`), corrige (repli R6 ignore quand le genre exclu porte encore une contribution vive ; `CaptureBase` garde `SettledValue` en filet de securite pour les cibles store aussi ; deux tests de regression, dont un ajoute a `VisualStatesTests` au-dela de la re-ecriture prevue), second tour CONFIRMED avec quatre P4 : le test « un seul run » ne detecte pas une mutation qui rendrait la valeur animee (la garde d'egalite de `HandleChanged` la neutralise ; le test `Margin_LocalWriteDuringTheTransition_Retargets_WithoutJumping` la detecte) ; une transition de couleur alloue 104 octets par tick (preexistant : `new MGSolidFillBrush` par ecriture, le test zero allocation porte sur `Margin`) ; un `Foreground` purement herite d'un `MGTextBlock` ne se re-cible pas en vol (limite non levee, documentee dans l'ADR).

`IUIStoreBackedAnimationTarget<T>.TryGetValueBelowAnimation(element, out T)` ajoute a l'interface et implemente sur les neuf cibles store (`BackgroundSlotTarget`, `ForegroundTarget`, `TextForegroundTarget`, `BorderBrushTarget`, `MarginTarget`, `PaddingTarget`, `MinHeightTarget`, `BackgroundGradientTarget`, `BackgroundDiagonalGradientTarget`) : chacune appelle `TryGetResolvedPilotValueExcluding` sur son (pilote, slot) avec `UIValueSourceKind.Animation`, puis convertit le type de stockage du pilote (`IFillBrush`, `Color?`, `IBorderBrush`, `Thickness`, `int?`) vers le type de valeur de la cible (`Color`, `UIGradientColors`...), rendant faux quand la conversion echoue (brush non uni sous une cible couleur, par exemple) exactement comme le ferait `GetValue` en levant si on l'appelait a la place. `ForegroundTarget` (pilote `Foreground`) n'herite d'aucune dormance particuliere car `MGElement.TryGetResolvedPilotValueCore` n'a de branche de dormance que pour `Background` et `DefaultTextForeground` (comme la methode existante) ; `MGTextBlock.cs`, hors perimetre, n'est pas touche, donc son repli Inherited/Theme (le chemin `TryGetResolvedPilotValue` surcharge pour `Foreground`) n'est pas reproduit ici -- decision assumee, sans consequence observee (le chemin `Foreground` n'est utilise que par `ForegroundTarget`, qui n'ecrit jamais que le slot `Normal`).

`UITransition<T>.HandleChanged` : la valeur que le run doit rejoindre (`underlying`) est desormais lue via `TryGetValueBelowAnimation` quand la cible est store-backee ET que c'est le run propre de la transition qui tourne (`ownRunActive`), secours sur `GetUnderlyingValue` sinon -- **decision** : une animation explicite etrangere en cours sur le meme chemin (`foreignAnimationOwnsPath`, branche « stay quiet, but follow its values ») garde volontairement la lecture physique (`GetUnderlyingValue`), jamais la lecture sous l'animation, car cette branche existe justement pour suivre la valeur de CETTE animation-la (pas celle en dessous) afin que la prochaine transition reparte d'ou elle s'est arretee ; sans cette exception le non-regression `ExplicitAnimation_StartedWhileATransitionRuns_StartsFromTheAnimatedValue` resterait correct mais le suivi post-animation casserait. Tous les gardes existants sont gardes tels quels (guard anti-auto-recible sur `Animation.CurrentValue` / `Animation.To`, guard `_HasSettledValue`) ; `ReadCurrentValue` de `UIPropertyAnimation<T>` n'est pas touche (ADR-0006). Chronologie constatee par instrumentation (les animations tickent en tete de frame, avant l'evaluation des etats visuels, ADR-0006 decision 7) : une ecriture non taguee sur le conteneur (mutation directe d'un sous-champ, cas `Background`) declenche une notification synchrone qui recible tout de suite, sans attendre un tick ; une ecriture taguee gatee par la precedence (`Margin`, `Padding`, `MinHeight`, ou tout slot pilote via son setter tague) n'emet aucune notification tant que le run garde la precedence, donc le retarget n'est vu qu'au tick suivant du run (qui, lui, ecrit toujours et notifie) ; une sortie d'etat nomme cumule les deux delais (le retrait de la contribution `VisualState` puis l'ecriture de la base sous la source du conteneur n'ont lieu qu'apres le tick d'animation de LEUR PROPRE frame), donc le recible n'est visible qu'au tick suivant, deux frames apres l'appel qui a fait sortir l'etat -- dans tous les cas, jamais de saut (le run reciblee part toujours de sa valeur animee courante) et jamais plus tard que la fin de l'ancien run (leve la limite V1).

Simplifications (etape 5 du brief), decidees au cas par cas apres lecture du code puis verification par la suite :
- `UIVisualStateApplier<T>.CaptureBase` **simplifiee** comme prevu : pour une cible store, la base est desormais `TryGetValueBelowAnimation` (secours sur la lecture existante si elle rend faux) -- correcte meme en plein run puisque les ticks du run ne touchent que sa propre contribution, donc le cas particulier de `SettledValue` est retire pour ces cibles. Pour une cible simple (aucun store a lire en dessous), le cas `SettledValue` reste necessaire tel quel et est conserve, avec le commentaire XML explicite disant pourquoi (sa valeur physique EST la valeur animee en plein run).
- `UIStoreBackedTargets.Restore` **non simplifiee** : le brief proposait de fusionner la branche `{Animation}` seule avec la branche « plus rien » (ecrire systematiquement la base sous la source du conteneur des qu'aucune contribution non-`Animation` ne reste). Verification faite (raisonnement puis lecture des tests existants) : cette fusion casse `VisualStatesTests.LeavingAStateOnAThemeBackground_PutsTheThemeColourBack`, qui exige explicitement une liste de contributions vide apres une sortie d'etat sans run en cours (`Assert.Empty(...EnumerateResolvedContributions...)`) -- or `WholeSource` lit le slot Whole du pilote, distinct du sous-slot restaure, et y trouverait la contribution Theme du conteneur, laissant une contribution Theme trainante sur le sous-slot au lieu de rendre la valeur CLR gardee sans aucune contribution. Le code existant (trois branches : autre contribution restante / seule `Animation` restante / rien) est donc garde tel quel ; seule la lecture (`TryGetValueBelowAnimation`) a change, pas cette ecriture. Suite complete verifiee verte avant et apres la tentative de fusion (`git diff` sur ce fichier limite au commentaire XML de la nouvelle methode d'interface).
- `VisualStatesTests.LeavingAStateMidTransitionOnAThemeBackground_ComesBackToTheThemeColour` re-ecrit (nom garde) : asserte desormais que la sortie en plein run fait repartir le run vers le noir des les deux frames suivantes (`SettledValue` et `Animation.To` deja au noir, la couleur dessinee n'a saute ni vers le blanc ni vers le noir), et l'atteint a la fin du nouveau run -- plus le retour "vu seulement a la fin du run" de l'ancienne version.

Nouveaux tests `TransitionTests.cs` : ecriture locale du fond pendant un run couleur (aucun saut, mouvement monotone vers la nouvelle cible, verifie par distance euclidienne RGB) ; meme scenario sur `Margin` (Thickness) ; sortie d'etat nomme en plein run assertee sur `SettledValue` / `Animation.To` / `IsRunning` de la transition ; non-regression de `UIPropertyAnimation<T>.ReadCurrentValue` (une animation explicite demarree pendant un run de transition part bien de la valeur animee, `StartValue` inchange) ; absence de fuite de run (un seul `transition:Margin` actif sur neuf frames sans ecriture externe, progression strictement croissante, meme objet `Animation`) ; zero allocation par tick d'une transition sur un pilote apres cinq frames de chauffe (`Margin`, choisi car son ecriture taguee ne boxe ni n'alloue de brush, contrairement aux cibles couleur -- cout documente et accepte ailleurs). Verification manuelle supplementaire (harnais, non commitee en test) des scenarios d'acceptation restants : ecriture locale egale a la cible courante (aucun recible, run inchange), deux ecritures locales consecutives (deux recibles, jamais plus d'une entree active), ecriture sous l'animation transition au repos (demarre un run normal comme avant). Suite complete 0 echec (2113 tests) ; `MGUI.Samples` compile (`-t:Compile`) ; `ResolvedPilotWriteSitesTests` inchange (aucun decalage de ligne necessaire).

**Fix round 1** (13 septembre 2026, verifie CONFIRMED puis corrige) : un `verifier` frais a trouve que la simplification de `CaptureBase` decrite ci-dessus n'etait correcte que quand le sous-slot portait deja une contribution non-`Animation` (le cas des deux tests commits, tous deux demarres par une ecriture d'etat). Quand le run est demarre par un echange du conteneur Whole (un changement de theme) plutot que par une ecriture de slot, le sous-slot ne porte, au moment ou un etat nomme est entre en plein run, QUE la contribution `Animation` de la transition elle-meme : `TryGetValueBelowAnimation` rendait alors `true` avec la valeur physique en plein vol (repli R6 de `TryGetResolvedBackgroundSubSlotValue` / `TryGetResolvedDefaultTextForegroundSubSlotValue` dans `MGElement.cs`, qui attribuait la valeur physique du conteneur au gagnant Whole sans verifier que le genre exclu portait lui-meme encore une contribution vivante sur ce sous-slot), et `CaptureBase` ne retombait plus du tout sur `SettledValue` pour une cible store (son repli etait garde `_StoreTarget == null`) -- la base memorisee restait alors une valeur en plein vol, et l'element y restait bloque apres la sortie de l'etat. Corrige par deux changements cibles : (1) les deux replis R6 sautent desormais le repli physique quand `TryGetResolvedContribution<T>(propriete, slot, exclu, out _)` trouve une contribution vivante du genre exclu sur ce sous-slot (nouvelle helper deja existante, aucune nouvelle API de store) ; (2) le repli `SettledValue` de `CaptureBase` n'est plus restreint aux cibles non-store, redevenant le filet de securite pour ce cas precis. Verifie par un test unitaire (`PilotValueBelowAnimationTests.Background_ExcludingAnimation_ReturnsFalse_WhenTheSubSlotsOnlyContributionIsAnimation`) et un test de bout en bout (`VisualStatesTests.EnteringAStateMidTransitionStartedByAWholeContainerSwap_ComesBackToTheSwappedInColour`), tous deux confirmes en echec sans le correctif (valeur figee sur du gris en plein vol) et verts avec. `UITransition<T>.HandleChanged` n'a pas change : son garde anti-auto-recible calcule la meme valeur `underlying` avant et apres le correctif dans ce cas precis, donc aucune regression de retarget. Suite complete verifiee verte : 2115/2115 (2113 + les deux nouveaux tests).

Commit recommande : `animation: retarget a pilot transition on a write made under its run`

### ✅ U4. Un etat nomme qui l'emporte sur une valeur locale

But : `UIVisualState.OverridesLocalValue`, palier `VisualStateOverride = 95`.

Etat actuel : voir « Precedence ».

Travail attendu :

- `UIValuePrecedence.VisualStateOverride = 95` (verification prealable : aucun switch exhaustif sur `UIValuePrecedence`) ; `UIVisualState.OverridesLocalValue` ; `UIVisualStateApplier<T>` construit la source avec ce palier quand le drapeau est vrai ; `VisualStateDefinition.OverridesLocalValue` en XAML ; `UIToolingService` rend la precedence (deja dans `UIValueResolutionSource`).
- Docs : « Etats visuels nommes » (le drapeau, ce qu'il passe : `LocalValue` et `LocalBinding`, ce qu'il ne passe pas : `Animation`).
- Tests `VisualStatesTests` (extension) : scenario de `ALocalValue_ShadowsANamedStateOnAPilot` avec le drapeau (l'etat gagne, source `VisualState` a 95), sans le drapeau (inchange), un run `Animation` gagne toujours, XAML round-trip du drapeau, `ResolvedValueSourceDiagnosticsTests` voit le palier.

Commit recommande : `animation: let a named visual state override a local value`

**Statut** (13 septembre 2026, livre) : `UIValuePrecedence.VisualStateOverride = 95` ajoute entre `LocalValue` (90) et `Animation` (100) (verification prealable faite : aucun `Enum.GetValues<UIValuePrecedence>`, `typeof(UIValuePrecedence)` ni cast `(UIValuePrecedence)` dans `MGUI.Tests`/`MGUI.Core`, donc aucune table a adapter). `UIVisualState.OverridesLocalValue` (bool, defaut faux) propage sa valeur a l'`Applier` de chaque setter deja present au moment ou elle change, et a tout setter ajoute ensuite (`Add`) : aucun changement au point d'appel `UIVisualStateCollection.Apply` (hors perimetre de la tache), qui continue de passer le seul nom d'etat -- le drapeau voyage avec le setter, pas avec l'appel. `UIVisualStateApplier<T>.Apply` construit la source directement (`new UIValueResolutionSource(...)`, la factory statique `VisualState(...)` restant fixee a 70) et invalide son cache (`_LastSource`/`_LastStateName`) aussi quand `OverridesLocalValue` differe de la derniere valeur mise en cache (`_LastOverrides`), pas seulement quand le nom d'etat change -- necessaire car re-entrer le MEME `UIVisualState` garde la meme reference de nom, donc sans ce second critere le palier ecrit resterait celui du premier `Apply`. Verification faite (lecture) que `Unset`/`ClearContribution` du store filtrent par `UIValueSourceKind`, jamais par la precedence : aucun changement a `UIResolvedPropertyStore.cs` ni `IUIStoreBackedAnimationTarget.cs`, conforme a l'hypothese du brief. `VisualStateDefinition.OverridesLocalValue` (XAML, defaut faux) transfere par `ToVisualState()` ; le chargeur strict valide deja les attributs par reflexion sur les proprietes publiques du DTO (`XamlLoaderDiagnostics.ValidateKnownAttributes`), donc aucun changement au loader n'etait necessaire, verifie par un test negatif (attribut mal orthographie rejete). `UIToolingService`/`UIElementDebugView` inchanges : `UIValueOriginView.Source` porte deja le `UIValueResolutionSource` complet (kind et precedence), verifie en lisant le code plutot qu'en le supposant. Revue independante (verifier) : trois tours. Tour 1 REFUTED (deux P1 : `UIResolvedPropertyStore.Set<T>` remplacait en place une contribution de meme genre sans re-trier quand sa precedence changeait, donc un etat `OverridesLocalValue` entrant ou sortant sur un chemin partage laissait un 70 au-dessus d'une valeur locale ; corrige par retrait puis re-insertion triee, plus les quatre gardes d'ecriture physique des sous-slots dans `MGElement` / `MGTextBlock`, perimetre etendu et documente dans l'ADR). Tour 2 REFUTED (P2 : la garde `effectiveChanged` seule ne re-synchronisait plus un sous-champ ecrit sans tag entre deux ecritures identiques du gagnant ; corrige par `effectiveChanged || meme genre`, test de regression). Tour 3 CONFIRMED avec deux P3 (cinq tirets cadratins de commentaires de `MGElement.cs` remplaces par U+FFFD par un outil non UTF-8, restaures avant commit ; perimetre etendu a `MGElement.cs` / `MGTextBlock.cs`, accepte) et deux P4 (basculer le drapeau sur un etat deja courant ne re-applique pas avant le prochain changement d'etat, comportement documente ; une instance de `UIVisualStateSetter` partagee entre deux etats porterait le drapeau du dernier `Add`, aucun partage dans le code).
- Tests ajoutes (`VisualStatesTests.cs`, huit) : l'etat gagne sur une `LocalValue` et restaure la valeur locale masquee a la sortie ; l'etat gagne sur une `LocalBinding` (ecriture taguee via `SetBackgroundSlot(..., UIValueResolutionSource.LocalBinding(...))`, motif deja utilise dans ce fichier) ; sans le drapeau la precedence rapportee reste 70 ; une `Animation` explicite (`UIPropertyAnimation<Color>`, `FillBehavior.RestoreBaseValue`) l'emporte toujours et la couleur de l'etat reapparait apres la restauration ; le cache s'invalide quand le drapeau change entre deux entrees du MEME `UIVisualState` (probe direct de la decision ci-dessus) ; dix allers-retours sans croissance du nombre de contributions (2 pendant, 1 apres) ; zero allocation sur 200 appels de `UIVisualStateCollection.Refresh()` a l'etat inchange (isole du bruit d'un `Desktop.Update()` complet, motif absent des tests zero-allocation existants qui appellent tous `UIAnimationManager.Update` directement -- celui-ci exerce `MGElement.Update`/`Refresh` a la place puisque c'est le chemin reel du refresh d'etat) ; round-trip XAML (element et style, defaut faux, attribut mal orthographie refuse en mode strict). `ResolvedValueSourceDiagnosticsTests.cs` (un ajout) : le palier 95 est rapporte par `TryGetResolvedValueSource`. Fix en cours d'ecriture (avant commit) : le premier jet du test "local value pendant l'etat actif" ecrivait par la facade non taguee (`BackgroundBrush.NormalValue = ...`), qui d'apres `MGElement.HandleBackgroundBrushContainerPropertyChanged` s'applique TOUJOURS physiquement quelle que soit la precedence (limite documentee, R5) -- corrige en ecrivant par `SetBackgroundSlot(..., UIValueResolutionSource.LocalValue(...))`, le chemin taguee qui applique reellement la porte de precedence ; capture avant commit, aucune regression a signaler. `ResolvedPilotWriteSitesTests.cs` inchange (aucune reference aux fichiers touches). Suite complete verifiee verte : 2124/2124 (2115 + 9 nouveaux tests) ; `MGUI.Samples` compile (`-t:Compile`).

**Fix round 1** (13 septembre 2026) : un verificateur independant a REFUTE la livraison (deux P1). Cause racine unique : `UIResolvedPropertyStore.Set<T>` (`UIResolvedPropertyStore.cs:149`) remplacait en place la contribution existante d'un `Kind` deja present sans jamais revalider sa position triee -- correct avant U4 (un `Kind` avait toujours une seule precedence), faux des que `VisualState` peut ecrire a 70 OU 95 selon l'etat courant, ce qu'`UIVisualStateCollection.Apply` (`UIVisualStateCollection.cs:158-163`) produit exactement lors d'une transition d'un etat vers un autre qui declare le MEME chemin (aucun `Restore` entre les deux, seul un second `Apply` ecrase la contribution). Corrige : `Set<T>` compare desormais la precedence de la contribution existante a celle de la nouvelle ecriture ; si elle differe, la contribution est retiree puis reinseree a la position que `FindInsertionIndex` lui donnerait (au lieu d'un remplacement en place), ce qui corrige les deux P1 (entrer dans l'etat prioritaire depuis un etat plat, et en sortir vers un etat plat) avec un seul changement, puisqu'ils partagent la meme cause. Effet de bord decouvert en ecrivant le test de regression et corrige dans le meme lot (hors perimetre initial mais necessaire a l'acceptation reelle, note ADR-0008) : la porte d'application physique des quatre ecritures de sous-slot taguees (`MGElement.SetBackgroundSlot`, `MGElement.SetBackgroundFocusedColor`, `MGElement.SetDefaultTextForegroundSlot`, `MGTextBlock.SetForegroundSlot`) verifiait `effective.Source.Kind == source.Kind` ("mon ecriture est-elle la gagnante ?"), une condition qui ne peut plus detecter le cas ou le tri ci-dessus fait gagner un AUTRE kind deja enregistre (ex. la valeur locale demasquee) -- remplacee par `effectiveChanged` (deja calcule par `Set<T>`, jusque-la ignore via `out _`), qui detecte tout changement du gagnant quel que soit son kind. Un test de regression est ajoute (`VisualStatesTests.LeavingAnOverridingStateForAPlainStateOnTheSamePath_NeverLeavesTheSeventyAboveALocalValue`) : etats `Checked` (override) et `Hover` (plat) partageant `Background`, verifie les deux sens de la transition (entrer dans l'override depuis le plat, en sortir vers le plat) contre une valeur locale taguee sous-jacente, avec assertion sur la couleur physique (`BackgroundBrush.NormalValue`), le `Kind`/la precedence rapportes et le contenu tri de `EnumerateResolvedContributions`. P4 du rapport de verification (aucun test ne combinait deux etats sur un meme chemin) ainsi couvert. Suite complete reverifiee verte : 2125/2125 (2124 + 1 nouveau test) ; build `MGUI.Core` et `MGUI.Tests` verts ; `MGUI.Samples` compile (`-t:Compile`).

**Fix round 2** (13 septembre 2026) : un second verificateur independant a REFUTE le round 1 (un P2). Cause : le gate `effectiveChanged` introduit au round 1 n'est PAS un sur-ensemble strict de l'ancien gate `effective.Source.Kind == source.Kind`, contrairement a la note de conception du round 1 -- quand le gagnant courant reecrit sa PROPRE valeur, inchangee (une animation qui s'est stabilisee, ou un etat reapplique qui pose de nouveau la meme couleur), `effectiveChanged` vaut faux, alors que l'ancien gate aurait quand meme resynchronise le champ physique. Sonde reproduite par le verificateur puis rejouee ici : `SetBackgroundFocusedColor(Rouge, source Animation)` (physique = Rouge) -> ecriture facade non taguee `toggle.BackgroundBrush.FocusedColor = Bleu` (applique physiquement, enregistre une contribution `LocalValue` dormante a 90 sous l'`Animation` a 100) -> `SetBackgroundFocusedColor(Rouge, source Animation)` de nouveau (le gagnant reecrit sa propre valeur inchangee) : le champ physique restait bloque sur le Bleu de l'ecriture non taguee au lieu de se resynchroniser sur le Rouge du gagnant. Les sous-slots compares par valeur (`Color?` : `Background.FocusedColor`, `DefaultTextForeground`, `MGTextBlock.Foreground`) y sont exposes facilement ; les sous-slots de type brush sont pour la plupart proteges par le `ReferenceEqualityComparer` utilise dans leur `Set<T>`. Corrige en elargissant les quatre portes concernees (`MGElement.SetBackgroundSlot`, `SetBackgroundFocusedColor`, `SetDefaultTextForegroundSlot`, `MGTextBlock.SetForegroundSlot`) a `effectiveChanged || effective.Source.Kind == source.Kind` : le OR reintroduit l'ancien gate comme filet de secours, de sorte que le gagnant peut toujours resynchroniser le champ physique sur sa propre valeur courante meme quand celle-ci vient d'etre reecrite sans changer. Verifie par un nouveau test de regression (`VisualStatesTests.TheWinnerRewritingItsOwnUnchangedValue_StillResyncsAStaleUntaggedSubField`), confirme en echec sans le correctif (rejoue manuellement en revenant temporairement a `effectiveChanged` seul sur `SetBackgroundFocusedColor`, `Assert.Equal` echoue avec Bleu au lieu de Rouge) et vert avec. Le test de regression du round 1 (`LeavingAnOverridingStateForAPlainStateOnTheSamePath_NeverLeavesTheSeventyAboveALocalValue`) reste vert avec le OR : l'ancien gate seul aurait deja suffi a ce scenario-la, le round 1 avait simplement remplace le mauvais operateur (un `&&`/substitution) au lieu d'ajouter le nouveau critere au lieu de l'ancien. Aucun changement a `UIResolvedPropertyStore.Set<T>` (le fix round 1 sur le tri reste correct, seul le fix round 1 sur les quatre portes de sous-slot etait incomplet). Constat P3 du rapport (fichiers hors perimetre) : inchange, deja documente ci-dessus et dans ADR-0008 -- `MGElement.cs`/`MGTextBlock.cs` restent modifies pour la meme raison qu'au round 1 (les quatre portes de sous-slot partagees par le framework), qui s'etend maintenant a ce fix. Constat P4 du rapport (drapeau bascule sans re-entrer l'etat) : non traite, hors P0-P2, avis conforme au comportement documente. Suite complete reverifiee verte : 2126/2126 (2125 + 1 nouveau test) ; build `MGUI.Core` et `MGUI.Tests` verts. BOM UTF-8 de `MGElement.cs` retrouve absent au debut de ce round (perdu avant ce round, cause non investiguee car hors perimetre) et restaure sans autre changement de contenu.

### ✅ U5. Slot Checked du toggle

But : `VisualStateBrush<T>.CheckedValue`, `MGToggleButton.CheckedBackgroundBrush` reel.

Etat actuel : voir « Checked ».

Travail attendu :

- `VisualState.cs` : `CheckedValue` sur `VisualStateBrush<T>` (copie par les constructeurs d'heritage, `Copy`), `GetValue(VisualState, bool isChecked)` ; `MGToggleButton` : `CheckedBackgroundBrush` / `CheckedTextForeground` lisent `CheckedValue ?? SelectedValue`, ecrivent `CheckedValue`, `DrawBackground` du toggle consomme `GetValue(state, IsChecked)` ; `IsChecked` continue de poser `IsSelected`.
- Docs : « Etats visuels nommes » (le slot `Checked` du toggle, limite : ni cible ni slot du store), `Docs/styling-theme-architecture.md` (mention).
- Tests `MGUI.Tests/Animation/ToggleCheckedSlotTests.cs` : toggle avec `CheckedBackgroundBrush` seul dessine cette brush coche ; toggle avec `SelectedBackground` seul inchange avant et apres ; `IsChecked` pose toujours `IsSelected` ; `MGCheckBox` et `MGRadioButton` inchanges (tests existants verts) ; l'etat nomme `Checked` V2 reste tel quel (`VisualStatesTests` verts).

**Statut** (13 septembre 2026) : livre. Mecanisme : `VisualStateBrush<T>.CheckedValue`/`HasCheckedValue` (nullable meme pour `T` struct via un drapeau explicite, `VisualStateColorBrush` inclus) et `GetValue(PrimaryVisualState/VisualState, bool isChecked)`, copies par les deux constructeurs d'heritage prives et inclus dans le passage de dedoublonnage de `VisualStateFillBrush.Update()` ; cote dessin, un seul point d'extension protege ajoute dans `MGElement` (`ResolveBackgroundUnderlay(VisualStateFillBrush, PrimaryVisualState)`, appele aux deux endroits de `DrawBackground` a la place de `BackgroundBrush.GetUnderlay(...)` direct), redefini par `MGToggleButton` pour appeler `brush.GetValue(state, IsChecked)` -- les neuf autres sites `GetUnderlay` du framework et le chemin generique restent intacts (verifie par grep). Decision prise pendant la livraison, hors options b/c du chapitre « Checked » de l'auteur : `CheckedBackgroundBrush` et `CheckedTextForeground` sont tous deux suivis par un champ prive sur le toggle (pas seulement par `BackgroundBrush.CheckedValue`), car `RefreshThemeBackgroundDefault` (execute par le framework juste avant `OnThemeChanged` a chaque changement de theme) peut deja avoir remplace le conteneur `BackgroundBrush` par un clone frais sans valeur coche avant meme que `MGToggleButton.OnThemeChanged` s'execute -- capturer/reappliquer la valeur depuis l'ancien conteneur a l'interieur de `OnThemeChanged` (option initialement prevue) arrive donc trop tard ; le champ sur le toggle survit a ce remplacement et `OnThemeChanged` le reapplique sans condition apres son propre `SetBackground`. `CheckedTextForeground` suit le meme principe car `DefaultTextForeground` est un `VisualStateSetting<Color?>`, pas un `VisualStateBrush<T>` : pas de `CheckedValue` propre, le champ du toggle est resynchronise sur le sous-slot `Selected` existant (`SetDefaultTextForegroundSlot`, meme pilote qu'avant cette tranche). Tests : dix dans `ToggleCheckedSlotTests.cs` (dessin reel checked/unchecked/repli sur cinq cycles, parite avec `GetUnderlay(Selected)` sans slot code, `IsChecked`/`IsSelected`/etat nomme `Checked`, texte checked resolu via `TryGetResolvedPilotValue`, survie a un vrai changement de theme pour le fond ET le texte, un toggle sans slot code suit le nouveau theme, copie de `VisualStateFillBrush`/`VisualStateColorBrush` avec `CheckedValue`, repli de `GetValue(state, isChecked)`, zero allocation sur 200 appels de `GetValue` a l'etat inchange). `MGCheckBox`/`MGRadioButton` : aucune nouvelle couverture, leur coche est dessinee par des elements enfants (`MGCheckStateIcon`, `MGRadioIndicatorIcon`/`MGRadioBulletIcon`), pas par `BackgroundBrush`/`DefaultTextForeground` ; suites existantes vertes. Suite complete verifiee verte : 2136/2136 ; `MGUI.Samples` compile (`-t:Compile`). `ResolvedPilotWriteSitesTests.cs` inchange (`CheckedValue` n'est ni `BackgroundBrush.SelectedValue` ni `DefaultTextForeground.SelectedValue`, hors du motif surveille ; `SetDefaultTextForegroundSlot(...)` reste un appel de methode, pas une affectation `DefaultTextForeground =`).

**Fix round 1** (13 septembre 2026, verificateur frais) : P2 corrige -- `CheckedBackgroundBrush` etait suivi uniquement par le champ prive du toggle et resynchronise seulement depuis `CheckedBackgroundBrush`'s setter et `OnThemeChanged` ; tout AUTRE remplacement complet du conteneur `BackgroundBrush` (le setter public de `MGElement.BackgroundBrush`, `MGExpander.ExpanderButtonBackgroundBrush`, ou une resolution pilote/style du slot Whole) perdait silencieusement `CheckedValue` sur le nouveau conteneur pendant que le getter continuait de renvoyer le champ, donc la propriete et le rendu divergeaient. Corrige par un hook protege virtuel supplementaire `MGElement.OnBackgroundBrushContainerReplaced()`, appele a la fin de `ApplyBackgroundEffective` (donc a chaque remplacement reel du conteneur, quel que soit l'appelant) ; `MGToggleButton` le redefinit pour rappeler `SyncCheckedBackgroundValue()`. L'appel explicite dans `OnThemeChanged` devient redondant (le changement de theme passe aussi par `ApplyBackgroundEffective` via `RefreshThemeBackgroundDefault`/`SetBackground`) et a ete retire. P3 (fond desactive-coche dessine la brush coche) laisse tel quel : conforme au brief, decision produit signalee a l'auteur, pas une regression de cette tranche. Nouveau test `ToggleCheckedSlotTests.ReassigningBackgroundBrush_AfterCheckedBrushIsSet_KeepsTheCheckedBrushDrawnAndReported` reproduit le scenario du verificateur (assigner un nouveau `BackgroundBrush` complet apres avoir pose `CheckedBackgroundBrush`) et verifie a la fois le rendu et la propriete. Suite complete reverifiee verte : 2137/2137 ; `MGUI.Samples` compile.

**Fix round 2** (13 septembre 2026, verificateur frais) : un P1 et un P2 corriges, la revendication du round 1 sur `CheckedTextForeground` ("n'avait besoin d'aucun correctif equivalent") etait fausse. P1 : `SyncCheckedTextForegroundSlot`, quand une valeur code est posee, ecrivait le sous-slot Selected avec la source telle quelle (`UIValueResolutionSource.Default` depuis le setter/le constructeur, `UIValueResolutionSource.Theme` depuis `OnThemeChanged`), toutes deux bien en-dessous de la precedence `LocalValue` (0 et 20 contre 90). Des qu'un changement de theme reel avait deja enregistre une contribution `Theme` sur ce slot (le seul ecrivain `Theme` sur ce slot pour un toggle etant cette meme methode, appelee depuis `OnThemeChanged`), une ecriture code posterieure a `Default` ne pouvait plus jamais l'emporter : le champ physique (`DefaultTextForeground.SelectedValue`) restait bloque sur la couleur du theme alors que le getter `CheckedTextForeground` (adosse au champ du toggle, pas au slot resolu) renvoyait bien la couleur code -- meme classe de divergence que le P2 du round 1, restee ouverte cote texte. Sonde : toggle affiche -> vrai changement de theme -> pose de `CheckedTextForeground` -> une frame -> `DefaultTextForeground.SelectedValue` restait la couleur du theme. Corrige en donnant a une valeur code une precedence reelle de priorite : la branche "valeur posee" ecrit desormais toujours via `UIValueResolutionSource.LocalValue(source.Invalidation)` (ne garde de `source` que son `Invalidation`), ce qui l'emporte sur `Theme`/`Style`/`Template`/`VisualState` quel que soit l'ordre d'ecriture -- coherent avec le setter public `DefaultTextForeground` lui-meme, qui utilise deja `LocalValue` pour une affectation directe. La branche "valeur absente" (repli) commence par vider toute contribution `LocalValue` residuelle via `MGElement.ClearPilotSource` (deja `internal`, aucune nouvelle methode necessaire), puis ecrit la couleur de repli du theme avec la `source` d'origine comme avant, pour que le retrait restaure bien le gagnant naturel au lieu de laisser une contribution `LocalValue` perimee bloquee en tete. P2 : `SyncCheckedBackgroundValue` (le nouvel appel du round 1, invoque sans condition depuis `OnBackgroundBrushContainerReplaced` a chaque remplacement du conteneur `BackgroundBrush`) dereferencait `BackgroundBrush` sans garde, levant une `NullReferenceException` sur `toggle.BackgroundBrush = null` meme pour un toggle n'ayant jamais utilise `CheckedBackgroundBrush` -- contrairement a `ReapplyBackgroundSubSlots`, la reapplication soeur pour le meme remplacement de conteneur, qui verifie deja `_backgroundBrush == null`. Corrige par la meme garde d'une ligne en tete de `SyncCheckedBackgroundValue`. Tests ajoutes : `RealThemeChange_AfterCheckedTextForegroundIsSetFirst_TheCodeColourStillWins` (ordre inverse du test existant : changement de theme *puis* pose de `CheckedTextForeground`, jamais exerce par les tests du round 1) et `BackgroundBrush_SetToNull_DoesNotThrow_EvenAfterCheckedBackgroundBrushWasUsed` (reproduit la sonde exacte du verificateur). P3 (fond desactive-coche) et P4 (dossier `.serena/` non suivi) laisses tels quels, hors P0-P2 et hors perimetre. Suite complete reverifiee verte : 2139/2139 (2137 + 2 nouveaux tests) ; `MGUI.Core`/`MGUI.Tests` build vert ; `MGUI.Samples` compile.

**Revue finale** (session principale, 13 septembre 2026) : troisieme tour du verifier CONFIRMED avec deux P3 et deux P4. P3 differe, point ouvert pour l'auteur : un toggle a la fois desactive et coche avec un `CheckedBackgroundBrush` pose par code dessine la brush cochee et non la brush `Disabled` (`GetValue(state, isChecked)` fait passer `Checked` devant `Disabled` ; avant U5, `Disabled` gagnait toujours ; n'affecte aucun theme ni XAML existant puisque le slot est code seulement). Si l'auteur veut que `Disabled` reste prioritaire, `GetValue(state, isChecked)` doit ignorer `CheckedValue` quand `state.Primary == Disabled`. P3 accepte : second hook virtuel `OnBackgroundBrushContainerReplaced` dans `MGElement` (vide par defaut, point unique de re-synchronisation apres tout remplacement du conteneur de fond). P4 : `MGElement.BackgroundUnderlay` (diagnostic, sans consommateur de rendu) rend toujours `GetUnderlay(Primary)` et non la brush cochee ; dossier `.serena/` non suivi (cache d'outil, hors depot).

Commit recommande : `animation: give the toggle button a real Checked brush slot`

### ✅ U6. Clip de keyframes multi-pistes

But : `UIKeyFrameClipDto`, `UIKeyFrameClipSerializer` -> `UIStoryboard`.

Etat actuel : voir « Composition ».

Travail attendu :

- `KeyFrames/UIKeyFrameClipSerializer.cs` (DTO, `Serialize(UIStoryboard)`, `Deserialize(json)`), `Format` / `Parse` de `UIKeyFrameSerializer` partages ; duree du clip faisant autorite ; refus explicites.
- Docs : « Keyframes » (format du clip).
- Tests `MGUI.Tests/Animation/KeyFrameClipTests.cs` : clip a trois pistes (float, Vector2, Color) joue en parallele comme le storyboard du sample, round-trip, chemin inconnu, type inconnu, version inconnue, enfant non keyframes refuse, zero allocation par tick du storyboard construit.

Commit recommande : `animation: add the multi-track keyframe clip format`

**Statut** (13 septembre 2026) : livre. `UIKeyFrameSerializer.Format` / `Parse` (et les options JSON deja privees) sont devenus `internal static`, plus un nouvel `internal static bool IsSupported(Type)` : comportement inchange, `Serialize<T>` / `Deserialize<T>` / `ReadValueType` publics et intacts (`KeyFrameTests` vert sans modification). `UIKeyFrameClipSerializer` : `Serialize(UIStoryboard)` verifie chaque enfant par reflection (type generique `UIKeyFrameAnimation<>`, pas de `Delay`, pas de repetition, pas d'aller-retour, `Property` connu de `UIAnimationTargets`, meme `Duration` que le clip) puis delegue a une methode generique privee invoquee par `MakeGenericMethod` (precedent `UITransition.Create`) pour lire `Track`/`Property` sans reflection supplementaire a l'interieur ; `Deserialize(json)` verifie chemin/type/cadres puis construit chaque `UIKeyFrameAnimation<T>` de la meme facon (`MakeGenericType`/`MakeGenericMethod`). Duree du clip a la serialisation : `storyboard.Duration` (deja calculee si le storyboard a ete joue) ou, par defaut (storyboard construit mais jamais demarre, `Duration` a zero), la plus longue piste -- decision prise pendant la livraison car `UIStoryboard.ComputeDuration()` est protege et ne peut pas etre appele depuis le serialiseur, et parce que la publication d'un getter public equivalent aurait elargi le perimetre au-dela de ce qui est demande. Deux pistes sur le meme chemin ne sont pas refusees explicitement : la regle de conflit existante du moteur (une animation active par proprietaire et par chemin, `UIAnimationManager.Start`) s'applique de facon deterministe (la piste demarree en dernier, dans l'ordre du fichier, gagne le conflit ; verifie par un test dedie). Docs `Docs/animation-architecture.md` section Keyframes (sous-section "Clip multi-pistes", exemple JSON valide verifie par un test de deserialisation reel). Tests : `MGUI.Tests/Animation/KeyFrameClipTests.cs` (15) -- lecture parallele des trois pistes (float, Vector2, Color) sur un element, round-trip build/serialize/deserialize/serialize identique, round-trip avec espaces et proprietes reordonnees (canonicalisation), round-trip Color avec alpha et piste Thickness (`Margin`), refus chemin inconnu (liste les chemins connus), refus decalage de type (`Int32` pour `Opacity`), refus type non supporte (`MinHeight`, cible `int?`, non gere par `Format`/`Parse`), refus version inconnue, refus pistes/cadres vides, refus cadres ne finissant pas a l'offset 1, refus enfant non-keyframes / enfant retarde / enfant a duree differente, deux pistes memes chemin (comportement deterministe verifie), zero allocation par tick d'un clip deserialise (pistes Opacity/RenderTransform.Scale/Margin -- `Background` volontairement exclue de cette mesure, car `BackgroundSlotTarget.SetValue` alloue un `MGSolidFillBrush` a chaque ecriture independamment de cette tranche, comme le confirme le choix de `Margin` par `TransitionTests.RunningPilotTransition_AllocatesNothingPerTick_AfterWarmUp` deja existant). Suite complete verifiee verte : 2154/2154 (2139 + 15 nouveaux tests) ; `MGUI.Samples` compile (`-t:Compile`).

**Correctif (13 septembre 2026, revue independante round 1)** : `SerializeChild` invoquait `SerializeTrack<T>` par reflection (`MakeGenericMethod`/`Invoke`) sans `try`/`catch`, contrairement a `DeserializeTrack` qui deballe deja `TargetInvocationException` -- tout refus leve a l'interieur de `SerializeTrack<T>` (chemin `Property` inconnu ou vide, `Track` manquant, `NotSupportedException` de `Format<T>` pour un type non supporte) remontait donc emballe dans une `TargetInvocationException` au lieu du type documente par le XML doc de `Serialize` (`InvalidOperationException` nommant index/chemin/raison). Corrige en ajoutant le meme `try { ... } catch (TargetInvocationException ex) when (ex.InnerException != null) { throw ex.InnerException; }` autour de l'appel dans `SerializeChild`. Trois tests ajoutes a `KeyFrameClipTests.cs` (18 au total) : chemin inconnu, `Property` vide, type non supporte par `Format<T>` (piste `bool` sur un chemin enregistre) -- chacun verifie desormais le type d'exception attendu (`InvalidOperationException` ou `NotSupportedException`, jamais `TargetInvocationException`). Suite complete reverifiee verte : 2157/2157.

**Revue finale** (session principale, 13 septembre 2026) : second tour du verifier CONFIRMED avec un P3 et deux P4. P3 differe vers U8 (meme fichier) : `Serialize` accepte un enfant dont la piste est vide et produit un document que `Deserialize` refuse (« has no frames ») ; `SerializeTrack<T>` ne garde que `Track == null`. P4 : `Serialize` ne croise pas le `T` d'une piste avec le type enregistre de la cible (asymetrie ecriture/lecture, une telle animation echoue de toute facon au demarrage) ; les cadres qui ne commencent pas a 0 et les noms d'easing inconnus sont toleres (comportement V2 preexistant de `UIKeyFrameTrack<T>.Validate` et `ResolveEasing`, inchange).

### ⚪ U7. Seek

But : `UIAnimation.Seek(TimeSpan)` et `UIAnimationPreview.Attach`.

Etat actuel : voir « Seek ».

Travail attendu :

- Passe de conception d'abord (statut de cette tache) : etat `Completed` et instances de preview, `FinishedChildren` des groupes, sweep du manager ; puis `UIAnimation.Seek`, `UIAnimationGroup.Seek`, `UIAnimationPreview.Attach(element, animation)` (sans manager), refus sur une animation enregistree.
- Docs : section « Preview et seek » (contrat : aucun evenement, aucune completion, hote responsable de l'element de preview).
- Tests `MGUI.Tests/Animation/SeekTests.cs` : `Seek` avant egale N `Advance` sur `Progress` et `CurrentValue` ; arriere depuis mi-course et depuis la fin ; aucun evenement ; storyboard et sequence positionnent leurs enfants ; refus sur une animation enregistree ; zero allocation par `Seek`.

Commit recommande : `animation: add seek for preview instances`

### ⚪ U8. Serialisation d'un storyboard

But : `UIAnimationNodeDto`, `UIAnimationSerializer`.

Etat actuel : voir « Outillage ».

Travail attendu :

- `KeyFrames/UIAnimationSerializer.cs` : DTO arbre versionne, `Serialize(animation, nameOf)`, `Deserialize(json, resolveElement)` ; point d'extension `Format` / `Parse` pour une cible applicative (erreur explicite sinon).
- Docs : « Keyframes » ou nouvelle section « Serialisation », precedent `GraphSerializer`.
- Tests `MGUI.Tests/Animation/AnimationSerializerTests.cs` : round-trip d'un storyboard (propriete, delai, keyframes, sequence imbriquee) avec durees, easings, fill, cancel, ordre ; version, kind et chemin inconnus refuses en listant les chemins connus ; cible applicative sans `Format` / `Parse` refusee explicitement.

Commit recommande : `animation: serialize storyboards to JSON`

### ⚪ U9. Refresh a chaud des transitions et etats des styles

But : `ElementStyleRefresher` re-transfere les transitions et etats des styles.

Etat actuel : voir « Refresh de styles ».

Travail attendu :

- Provenance sur `UITransition` et `UIVisualState`, `ElementStyleScope.OwnTransitionPaths` / `OwnVisualStateNames`, passe de refresh dans `RefreshElement`, `MGElement.RefreshedStyleTransitionPaths` / `RefreshedStyleVisualStateNames`, compteurs de `UIStyleRefreshResult`.
- Docs : `Docs/animation-architecture.md` (« Styles et themes » : la limite « pas de refresh a chaud » disparait), `Docs/styling-theme-architecture.md` (« Refresh de styles a chaud »).
- Tests `MGUI.Tests/Architecture/StyleRefreshTests.cs` (extension) : transition de style ajoutee apres chargement atteinte par `RefreshStyles` ; retiree -> retiree de l'element, run en cours garde sa valeur ; etat nomme remplace re-applique s'il est courant ; declaration de l'element jamais touchee ; deux refreshs sans changement n'ecrivent rien ; style implicite du desktop fusionne pris en compte.

Commit recommande : `styling: refresh style transitions and visual states at runtime`

### ⚪ U10. Migrations : texte revele et brush de surbrillance

But : `MGTextBlock.TextCharactersPerSecond` sur le moteur, `MGHighlightBorderBrush` sur l'horloge du desktop.

Etat actuel : voir « Consommateurs de temps ».

Travail attendu :

- Cible `TextBlock.TextProgress`, `MGTextBlock.SyncTextProgressAnimation`, `ApplyAnimatedTextProgress`, remise a zero sur `Text` ; `UpdateSelf` ne cumule plus.
- `UpdateBaseArgs.AnimationDeltaTime` (`MGUI.Shared`), pose par `MGDesktop.Update` ; `MGHighlightBorderBrush.Update` le consomme.
- Docs : « Cibles », nouvelle section « Migrations » (ce qui suit l'horloge du desktop, ce qui garde sa propre horloge et pourquoi).
- Tests `MGUI.Tests/Animation/TextProgressAnimationTests.cs` : fin du reveal a la frame attendue, changement de vitesse en cours, changement de `Text` qui repart de zero, detachement qui garde puis reprise, pause et `TimeScale` du desktop appliques, zero allocation par tick ; `HighlightBorderBrushClockTests.cs` : progression par le delta mis a l'echelle, brush sans `Target` qui avance.

Commit recommande : `animation: drive the text reveal and the highlight brush with the engine clock`

### ⚪ U11. Sample, scenario, documentation et ADR

But : colonne V3 du sample (`SCN-ANIM-003`), completion de l'ADR-0008.

Travail attendu :

- `MGUI.Samples/Features/AnimationDemo.xaml(.cs)` : easing de Bezier en XAML, toggle avec `CheckedBackgroundBrush` et un etat `OverridesLocalValue`, clip JSON charge en storyboard, preview avec `Seek` pilote par un slider, texte revele qui suit la pause de l'horloge, brush de surbrillance qui suit `TimeScale` ; `Docs/scenario-validation-index.md` : `SCN-ANIM-003` ; `AnimationDemoSampleTests` : section V3 ; ADR-0008 : statut Accepted et « Decisions taken during delivery » ; `Docs/animation-architecture.md` : « Reste a faire » mis a jour.

Commit recommande : `animation: add the V3 sample, scenario and documentation`
