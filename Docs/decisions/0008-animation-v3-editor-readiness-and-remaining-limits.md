# ADR-0008: Animation V3: editor readiness, remaining pilot limits and ad hoc migrations

- **Status**: Proposed (2026-09-13, waiting for the author's answers to the ten questions of `Docs/Tasks/animation-v3-tasks.md`)
- **Date**: 2026-09-13
- **Source**: this chantier: read-only audit of the V3 scope deferred by ADR-0007 ("Deferred (V3 or later)") and of the two V2 limits recorded in its "Decisions taken during delivery", against HEAD `e52880c`; program plan `Docs/Tasks/animation-v3-tasks.md`

## Context

Facts verified at HEAD `e52880c` (read-only audit by three independent readers, V1 and V2 delivered by ADR-0006 and ADR-0007):

- Easings are resolved by name only: `UIEasing` (`MGUI.Core/UI/Animation/Easing/UIEasing.cs`) keeps nineteen `DelegateEasing` instances in a case-insensitive registry, `TryGet(name)` is a plain dictionary lookup (`:99`), `Register(name, function)` is the extensibility point (`:83`). The four consumers (XAML `Transition.Easing`, `MGThemeAnimationSettings.*Easing` strings, keyframe JSON `Easing`, fluent `Ease(string)`) all go through `TryGet`; no Bezier code exists under `Animation/`.
- Composition and keyframes are independent: `UIAnimationGroup` (`Composition/UIAnimationGroup.cs`) writes no property (`IsStoreBacked` false, `BaseValueBoxed` null, synthetic `TargetKey`) and only decides when to start its children, each ticked by the manager through its own delta-based `Advance`; `UIKeyFrameAnimation<T>` drives one path; `UIKeyFrameSerializer` serialises one `UIKeyFrameTrack<T>` per document (`UIKeyFrameTrackDto { Version, ValueType, Frames }`, no path, no shared duration). `MGUI.Samples/Features/AnimationDemo.xaml.cs` already hand-builds the storyboard-of-keyframes shape a timeline editor emits.
- No seek: `UIAnimation.Advance(TimeSpan delta)` is `internal` and incremental (`Elapsed += delta`), early-returns unless `Running` or `Delayed` (`UIAnimation.cs:215-220`); `InternalsVisibleTo` covers `MGUI.Tests` and `CasaEngine.Tests` only (`MGUI.Core/Properties/AssemblyInfo.cs:6-7`). A headless replay from zero through `IUIDesktopRuntime.ApplyFrame` reaches a time forward only, re-fires every event and re-lays out at every frame for a layout pilot.
- Applicability of a target is a runtime exception: `ForegroundTarget.Require` (`Targets/UIColorAnimationTargets.cs:142-144`) and `ProgressButtonValueTarget.Require` (`Targets/UIBuiltInAnimationTargets.cs:178`) cast and throw; `UIAnimationTargets` exposes `Paths` and `GetValueType` but no owner type; `IUIAnimationTarget<T>` has `Path` and `IsStoreBacked` only.
- Store-backed targets read the physical value: the four colour targets' `GetUnderlyingValue` forward to `GetValue` (`UIColorAnimationTargets.cs:94, 122, 157, 195`), so while an `Animation` (100) contribution runs, `UITransition<T>.HandleChanged` (`UITransition.cs:170-177`) sees the in-flight value and a `LocalValue` (90) write recorded meanwhile stays invisible until the run ends. `UIResolvedPropertyStore.TryGetWinner<T>` (`Styling/UIResolvedPropertyStore.cs:232`) returns the first precedence-sorted contribution; there is no "winner excluding a kind" query. Two workarounds exist: `UIVisualStateApplier<T>.CaptureBase` reads the attached transition's `SettledValue` (`States/UIVisualState.cs:200-206`), and `UIStoreBackedTargets.Restore` parks the base under the container's source while only `Animation` remains (`IUIStoreBackedAnimationTarget.cs:74-78`).
- Precedence (`Styling/UIValuePrecedence.cs:11-15`): `Template` 60, `VisualState` 70, `LocalBinding` 80, `LocalValue` 90, `Animation` 100; a state setter on a pilot is written at 70 (`UIVisualState.cs:187`) and a local value shadows it, locked by `VisualStatesTests.ALocalValue_ShadowsANamedStateOnAPilot`.
- `PrimaryVisualState` has four members (`VisualState.cs:17-27`); `MGElement.ResolvePrimaryVisualState` (`:2898-2916`) knows no `Checked`; `UIValueSlot` and `UIResolvedPropertyStore.SlotCount = 6` mirror the four sub-fields; only `MGToggleButton.IsChecked` writes `IsSelected` (`MGToggleButton.cs:93`), and its `CheckedBackgroundBrush` / `CheckedTextForeground` are documented aliases of the `Selected` slots (`:46-70`); `MGCheckBox` and `MGRadioButton` draw their checked look through child elements and never touch `IsSelected`. The V2 named state `Checked` (`UIVisualStateNames.Checked`, `IUICheckable`) already gives any element an animatable, store-integrated checked look.
- Hot style refresh is setter-only: `ElementStyleScope` (`XAML/ElementStyleScope.cs:9-44`) keeps neither the element's own `Transitions` / `VisualStates` nor the styles' animation lists; `ElementStyleRefresher.RefreshElement` (`:176-288`) collects `Setter` objects only; transitions and states are transferred once in `Element.ApplyBaseSettings` (`Element.cs:619-645`); `UITransition` and `UIVisualState` carry no provenance; `MGResources.OnStyleAdded` / `OnStyleRemoved` have no subscriber (pull model, `element.RefreshStyles()`).
- Ad hoc time consumers: `MGTextBlock.UpdateSelf` ramps `TextProgress` with `FrameElapsed * TextCharactersPerSecond / NumCharacters` (`MGTextBlock.cs:1398-1404`), reset only by the constructor and the `TextCharactersPerSecond` setter (`:711`, `:1237`), not on a `Text` change; `MGHighlightBorderBrush.Update` adds `UA.FrameElapsed / CycleDuration` (`Brushes/Border Brushes/MGHighlightBorderBrush.cs:504`) and is a shared, deduplicated paint-time brush, not an element; `MGTimer` (`:272-273` Flicker on `Opacity`, `:332` countdown with its own `TimeScale`, `:339-341` Shake), `MGStopWatch` (`:237`), the tooltip hover delay (`MGElement.cs:3727-3746`, wall clock), `MGTextCaret` blink (`Text/MGTextCaret.cs:370-371`, wall clock) and `MGButton.IsRepeatButton` (`MGButton.cs:318-345`) are timers. `MGProgressBar`, `MGScrollViewer`, `MGSpoiler`, the docking auto-hide drawer and the graph zoom consume no time.
- Tooling: `UIToolingService.CaptureElementDebugView` reports the element's animations, transitions and named state (read-only, current frame); `MGDesktop.Animations` exposes `Clock`, `ActiveCount`, `ActiveAnimations`.

## Decision

Proposed program (each point is a task of `Docs/Tasks/animation-v3-tasks.md`, open questions there):

1. Bezier easings: `UICubicBezierEasing(x1, y1, x2, y2)` (CSS `cubic-bezier` semantics, Newton iterations with a bisection fallback, `X1` / `X2` clamped to [0,1], `Y1` / `Y2` free like `BackOut`), parsed from `cubic-bezier(x1,y1,x2,y2)` by `UIEasing.TryGet` when the name lookup misses, cached in the registry under its literal. No DTO changes: XAML, theme, keyframe JSON and the fluent API accept it at once.
2. Applicability metadata: `IUIAnimationTarget<T>.RequiredOwnerType` (default interface member, null = any element) filled by the targets that cast today, and `UIAnimationTargets.GetOwnerType(path)`, so an editor lists the paths a selected element accepts without trial and error.
3. Store: `UIResolvedPropertyStore.TryGetWinnerExcluding<T>(property, slot, kind)`, exposed to the store-backed targets as `TryGetValueBelowAnimation`, used by `UITransition<T>.HandleChanged` for store-backed targets only, so a write made under a running transition retargets it at once; the two workarounds (`CaptureBase`, the `{Animation}` branch of `UIStoreBackedTargets.Restore`) are then simplified. `UIPropertyAnimation<T>.ReadCurrentValue` keeps reading the animated value (an explicit animation starts from the current value, ADR-0006).
4. Local value versus state: `UIVisualState.OverridesLocalValue` (default false), written at a new `UIValuePrecedence.VisualStateOverride = 95` with `Kind` still `VisualState`, exposed in XAML on `VisualStateDefinition`; the global order of ADR-0005 is untouched.
5. `Checked`: no new `PrimaryVisualState` member and no new store slot (the V2 named state already covers the general case); `VisualStateBrush<T>.CheckedValue` (fill and colour brushes only), `MGToggleButton.CheckedBackgroundBrush` / `CheckedTextForeground` backed by it with the `Selected` slot as fallback so existing themes and XAML keep their look; `MGCheckBox` and `MGRadioButton` unchanged; `IUICheckable` stays the source of truth.
6. Keyframe clip: `UIKeyFrameClipDto { Version, Duration, Tracks[] { Property, ValueType, Frames[] } }` and `UIKeyFrameClipSerializer` building a `UIStoryboard` of `UIKeyFrameAnimation<T>` for one element (shared duration authoritative, `Format` / `Parse` of `UIKeyFrameSerializer` shared). Time remap of a composite's progress is explicitly out of scope.
7. Seek: public `UIAnimation.Seek(TimeSpan elapsed)` factoring the progress and iteration maths of `Advance`, one `ApplyProgress`, no `Started` / `Repeated` / `Completed` event, forward and backward, allowed on a composite (positions every child); reserved to preview instances driven by the host, never to an animation registered with a live manager (design pass on the `Completed` state before implementation).
8. Storyboard serialisation: `UIAnimationNodeDto` tree (`Kind`, `Path`, `Duration`, `Delay`, `Repeat`, `AutoReverse`, `FillBehavior`, `From`, `To`, `Easing`, `Track`, `Children`) and `UIAnimationSerializer.Serialize(animation)` / `Deserialize(json, Func<string, MGElement> resolveElement)` next to the keyframe serialiser, same versioning and the same closed set of value types (an application target needs a registered `Format` / `Parse` pair).
9. Hot refresh of style transitions and states: provenance on `UITransition` and `UIVisualState` (the style kind that produced them), `ElementStyleScope.OwnTransitionPaths` / `OwnVisualStateNames`, a refresh pass mirroring the setter pass (implicit then named styles), replacing or removing only style-owned entries, counters on `UIStyleRefreshResult`; last task of the program.
10. Migrations: `MGTextBlock.TextProgress` on the engine (`TextBlock.TextProgress` target, `SyncTextProgressAnimation` from the `Text` and `TextCharactersPerSecond` setters, characters per second kept as the public contract, progress reset on a `Text` change) and `MGHighlightBorderBrush` on the desktop clock through `UpdateBaseArgs.AnimationDeltaTime` (scaled delta set by `MGDesktop.Update` after the animations tick); `MGTimer`, `MGStopWatch`, the tooltip delay, the caret blink and the repeat button keep their own time source (ADR-0006 decision 7).

Out of scope (the author's game engine): the timeline widgets, the scrubber, the curve editing UI, undo / redo, project files, element lookup by name.

## Consequences

- The engine becomes tool-ready without hosting any tool: paths per element type, a seek that fires nothing, a clip and a storyboard format, Bezier curves as text.
- A transition on a pilot reacts to a write made during its run; the "seen at the run's end" limit of V1 disappears for transitions.
- A state can be declared to win over a local value on the elements where that is the intended look; nothing changes by default.
- `Checked` keeps its V2 shape (named state); the built-in toggle stops overloading `Selected` for two meanings without touching the enums or the store layout.
- The typewriter reveal and the highlight brush follow `MGDesktop.Animations.Clock` (pause, time scale): a behaviour change to announce.
- Accepted costs: one registry entry per distinct Bezier literal; one `Type` per target; a linear scan of at most eleven contributions per transition notification on a pilot; provenance fields on transitions and states.
- Deferred again: time remap of composites, CSS named Bezier presets, `MGTimer` effects on the engine, a predicate-based applicability.

## Decisions taken during delivery

- Bezier (U1, 13 September 2026): the registry key for a resolved literal is the caller's text after `Trim()` only, never its canonical form — `cubic-bezier(0.42,0,1,1)` and `cubic-bezier(0.42, 0, 1, 1)` cache as two distinct entries with equal curves, so `Names` can grow one entry per distinct spelling an application or an editor ever passed, not one per distinct curve; this was already the plan's stated trade-off (decision 1's "one registry entry per distinct Bezier literal"), made concrete here as a `Trim`-only key. `UICubicBezierEasing.Parse` throws `FormatException` (not `ArgumentException`) to match the vocabulary of a text-to-value conversion failure, distinct from the constructor's `ArgumentOutOfRangeException` on an out-of-range or non-finite control point. `EasingTests.Names_ListsTheNineteenBuiltIns` needed no rewrite: it already asserted a subset (`BuiltInNames.All(UIEasing.Names.Contains)`), so it tolerates the registry growing with resolved literals without becoming order- or count-dependent under parallel test classes. Fix round 1 (13 September 2026): `TryParse` validated only `x1`/`x2` before constructing, so a non-finite `y1`/`y2` (`NaN`, `Infinity`, or an overflowing exponent such as `1e40`) reached the constructor's `RequireFinite` and threw `ArgumentOutOfRangeException`, breaking `TryParse`/`TryGet`'s documented never-throws contract and turning `Parse`'s `FormatException` into the wrong exception type. `TryParse` now also rejects a non-finite `y1`/`y2` before constructing, alongside the existing `x1`/`x2` unit-interval check.
- Applicability (U2, 13 September 2026): the registry entry became a private `Entry(Target, ValueType, OwnerType)` record built once at `Register<T>` (which knows `typeof(T)` and reads `target.RequiredOwnerType`), replacing `GetValueType`'s per-call reflection over the target's interfaces with a stored field; `GetOwnerType` and the new `IsApplicable` follow the same null-safe, case-insensitive, non-throwing contract as `GetValueType`. Only two targets declare a non-null `RequiredOwnerType`: `ForegroundTarget` (`MGTextBlock`) and `ProgressButtonValueTarget` (`MGProgressButton`), the two whose `Require` helper casts and throws. `BorderBrushTarget` was read closely per the brief's instruction and disposed as `null`: its `RequireBorder` calls `element.GetBorder()`, a virtual facade that `MGButton`, `MGWindow`, `MGListBox`, `MGComboBox` and most bordered controls override to return their own internal `MGBorder`, and that `MGBorder` itself overrides to return `this` — not a strict cast to `MGBorder`, so any element exposing a border accepts the path; declaring `MGBorder` here would have been a bug (it would exclude every one of those controls, which is exactly what the path is for). `UIElementDebugView.ApplicablePaths` is an `init` property added to the existing positional record, not a new positional parameter: no test in `MGUI.Tests` constructs the record positionally, and an `init` property keeps every existing call site untouched.
- Value below the animation (U3, 13 September 2026): `MGElement.TryGetResolvedPilotValue<T>` was refactored into one private core (`TryGetResolvedPilotValueCore<T>(property, slot, UIValueSourceKind? excluded, out UIResolvedValue<T>)`) shared with the new `TryGetResolvedPilotValueExcluding<T>` (`excluded` set, returns the plain `T` rather than `UIResolvedValue<T>` since that is exactly what a `TryGetValueBelowAnimation` implementation needs), so the two paths — and their Background/DefaultTextForeground dormancy branches — cannot diverge; a small private relay (`TryGetStoreWinner<T>`) picks `TryGetWinner` vs `TryGetWinnerExcluding` on the store, avoiding a conditional `out var` the compiler's definite-assignment analysis cannot follow through a short-circuited `&&`. `MGTextBlock.cs` (out of this slice's perimeter) was deliberately left untouched: its `TryGetResolvedPilotValue` override adds an Inherited/Theme fallback chain for the `Foreground` pilot that `TryGetResolvedPilotValueExcluding` does not reproduce, so `ForegroundTarget.TryGetValueBelowAnimation` reads the plain store winner below `Animation` for the `Normal` slot, exactly like `MGElement`'s own (non-overridden) `TryGetResolvedPilotValue` would for that pilot — the Inherited/Theme fallback only exists on the overridden path, so this is not a behaviour gap, just an intentionally unreplicated override.
- Value below the animation (U3, continued): `UITransition<T>.HandleChanged` reads the value a running transition must settle to via `TryGetValueBelowAnimation` (falling back to `GetUnderlyingValue`) **only** while the transition's own run is active (`ownRunActive`); a foreign explicit animation holding the same path (`foreignAnimationOwnsPath`, the pre-existing "stay quiet, but follow its values" branch) keeps reading the physical value instead — that branch exists specifically to track the *other* animation's value so the next transition starts where it left off, and reading below it would defeat that purpose while leaving `ExplicitAnimation_StartedWhileATransitionRuns_StartsFromTheAnimatedValue` accidentally still green (a P1-shaped correctness gap the plan's phrasing did not call out explicitly, caught by tracing the "stay quiet" branch during implementation). Every existing guard (the self-tick guard on `Animation.CurrentValue`/`Animation.To`, the `_HasSettledValue` guard) is unchanged; `UIPropertyAnimation<T>.ReadCurrentValue` is untouched (ADR-0006). Measured (not merely reasoned) frame timing, since animations tick at the head of the frame before visual states are evaluated (ADR-0006 decision 7): a non-tagged container write (e.g. `element.BackgroundBrush.NormalValue = x`) retargets synchronously, same statement; a tagged, precedence-gated pilot write (`Margin`, `Padding`, `MinHeight`, any pilot's tagged setter) retargets on the run's own next tick (a gated write raises no notification by itself); a named-state exit retargets two ticks after the call that triggers it (the base is recorded only after that frame's own animation tick, so a second tick is needed to see it) — in every case, no jump (the retargeted run always starts from its current animated value) and never later than the old run's own end (the V1 "seen at the end of the run" limit is lifted, not merely shortened).
- Value below the animation (U3, simplifications): `UIVisualStateApplier<T>.CaptureBase` was simplified for store-backed targets exactly as planned (`TryGetValueBelowAnimation`, falling back to the previous read when it returns false), dropping the `SettledValue` special case for those targets since the new read stays correct mid-run; the special case is kept, and documented as still needed, for plain (non-store) targets, whose physical value *is* the animated one while a run is in flight. `UIStoreBackedTargets.Restore`'s three-way branch (another contribution remains / only `Animation` remains / nothing remains) was **not** simplified, unlike the plan's proposal to merge the last two into "park the base under the container source whenever no non-`Animation` contribution remains": tracing that merge against `VisualStatesTests.LeavingAStateOnAThemeBackground_PutsTheThemeColourBack` (no run in flight, plain state exit) showed it would leave a stray `Theme` contribution on the exited sub-slot instead of the empty list that test asserts (`WholeSource` reads the pilot's Whole slot, not the sub-slot being restored, and a Theme-written container has its own Whole contribution to surface there) — reverted before committing; only the *read* side (`TryGetValueBelowAnimation`) changed, not this write path. Both decisions were verified by running the full suite with and without the attempted change, not by reasoning alone.
- Value below the animation, Fix round 1 (13 September 2026): the "falling back to the previous read when it returns false" claim above did not hold for store-backed targets before this fix — `CaptureBase`'s `SettledValue` fallback was gated `_StoreTarget == null`, so a store target that failed `TryGetValueBelowAnimation` fell straight through to the plain physical read (`GetUnderlyingValue`/`GetValue`), the very in-flight value the U3 read was meant to avoid. `TryGetValueBelowAnimation` itself could also wrongly return **true** with that in-flight value: `TryGetResolvedBackgroundSubSlotValue`/`TryGetResolvedDefaultTextForegroundSubSlotValue`'s physical-fallback branch (`MGElement.cs`, R6) attributed the container's current physical sub-field to the Whole winner's source whenever a Whole winner existed under the exclusion, without checking whether the *excluded* kind itself still had a live contribution on that very sub-slot — exactly the configuration a Whole-container swap (a theme change) produces: the transition's `Animation` contribution is the sub-slot's only contribution, so excluding it should read "nothing recorded", not the physical field the animation is actively driving. Both methods now skip that physical fallback when `TryGetResolvedContribution<T>(property, slot, excluded.Value, out _)` finds a live contribution of the excluded kind on the sub-slot, via the existing `TryGetResolvedContribution` helper (no new store API); `CaptureBase`'s `SettledValue` fallback is now unconditional (store-backed or not) so this now-correctly-`false` case is caught the same way a plain target's mid-run capture always was. Reproduced directly (unit-level `TryGetResolvedPilotValueExcluding` on an Animation-only sub-slot, `PilotValueBelowAnimationTests.Background_ExcludingAnimation_ReturnsFalse_WhenTheSubSlotsOnlyContributionIsAnimation`) and end-to-end (`VisualStatesTests.EnteringAStateMidTransitionStartedByAWholeContainerSwap_ComesBackToTheSwappedInColour`: a Whole theme swap starts the run, `Checked` is entered mid-run, the base is captured then, and after exit and the run's natural end the background settles on the swapped-in colour, not the in-flight one) — both fail without the fix and pass with it; full suite green (2115/2115) with the fix. `UITransition<T>.HandleChanged` needed no change: its own self-tick guard (`_Comparer.Equals(underlying, Animation.CurrentValue)`) already made the previously-wrong `true` result and the corrected `false` result compute the same `underlying` value in that branch, so retargeting behaviour is unaffected — only `CaptureBase` (a visual state's base) was ever reading a genuinely wrong value.
- Local value versus state (U4, 13 September 2026): the propagation of `UIVisualState.OverridesLocalValue` to its setters' appliers happens entirely inside `UIVisualState` (on the property setter, for every setter already present, and on `Add`, for one added later) rather than by changing what `UIVisualStateCollection.Apply` passes to `UIVisualStateSetter.Applier.Apply` — that call site (`States/UIVisualStateCollection.cs:170`) stays outside this slice's perimeter and is untouched; the flag travels with the setter's applier, not with the per-frame call. `UIVisualStateApplier<T>.Apply` therefore builds its `UIValueResolutionSource` with the constructor directly (`new UIValueResolutionSource(UIValueSourceKind.VisualState, precedence, ...)`) instead of the `UIValueResolutionSource.VisualState(...)` static factory, which stays fixed at precedence 70 and untouched (out of perimeter). The applier's cache (`_LastSource`, keyed by `_LastStateName` via reference equality) gained a second key, `_LastOverrides`: re-entering the *same* `UIVisualState` instance after only toggling its flag keeps the state name's reference identical, so a name-only cache key would silently keep serving the stale precedence — verified by a dedicated test (`TogglingTheFlagBetweenTwoApplications_ChangesThePrecedenceWritten`) that fails without the second key and passes with it. Read-only verification, no code change: `UIResolvedPropertyStore.Unset<T>` and `IUIStoreBackedAnimationTarget<T>.ClearContribution`/`UIStoreBackedTargets.Restore` all match a contribution by `UIValueSourceKind`, never by `UIValuePrecedence` — a contribution written at 95 with `Kind = VisualState` is found and removed by the same code path as one written at 70, so `UIResolvedPropertyStore.cs` needed no change, confirmed by reading the store before touching anything and by the full test suite passing unchanged. Read-only verification, no code change: the strict-mode XAML loader (`XamlLoaderDiagnostics.ValidateKnownAttributes`) already validates every element's attributes by reflecting over the element type's public instance properties, so adding the plain `bool OverridesLocalValue { get; set; }` property to `VisualStateDefinition` was sufficient for the attribute to load and for a misspelled attribute to be rejected in strict mode — no loader change, verified by a negative test (`OverridesLocalValues` typo). Read-only verification, no code change: `UIToolingService`'s `UIValueOriginView.Source` already carries the full `UIValueResolutionSource` (kind and precedence) captured by `TryGetResolvedValueSource`, so the diagnostic already reports 95 for an overriding state without any tooling change, confirmed by a dedicated `ResolvedValueSourceDiagnosticsTests` addition instead of only reasoning about the existing code. Test-writing correction made before commit (not a design decision, recorded so the next reader is not surprised by the first draft): the "a local value written while the override is active stays hidden" scenario cannot use the raw `element.BackgroundBrush.NormalValue = x` facade, because an untagged sub-slot write is a documented limitation that always applies physically regardless of precedence (`MGElement.HandleBackgroundBrushContainerPropertyChanged`, R5) — it must use the tagged `SetBackgroundSlot(slot, value, UIValueResolutionSource.LocalValue(...))` path, which is the one that actually gates the physical write on the precedence comparison; the test was corrected to do so before being committed. Full suite verified green: 2124/2124 (2115 + 9 new tests, `VisualStatesTests.cs` eight, `ResolvedValueSourceDiagnosticsTests.cs` one); `MGUI.Samples` compiles (`-t:Compile`); `ResolvedPilotWriteSitesTests.cs` needed no line-shift adjustment (it references none of the files this slice touched).
- Local value versus state, Fix round 1 (13 September 2026): an independent verifier REFUTED the delivery above (two P1) — the "`UIResolvedPropertyStore.cs` needed no change" claim only checked `Unset`/`ClearContribution` (which match by `Kind`, unaffected) and missed `Set<T>` (`UIResolvedPropertyStore.cs:149`), which replaces an existing same-`Kind` contribution **in place**, without re-running `FindInsertionIndex`. Before U4 one `Kind` always carried one fixed precedence, so an in-place overwrite could never leave the sorted list out of order; U4 made `VisualState` carry two (70 plain, 95 overriding), and `UIVisualStateCollection.Apply` (`States/UIVisualStateCollection.cs:158-163`, itself out of this slice's perimeter and untouched) does not `Restore` a setter whose path the next state also declares — so a transition straight from an overriding state to a plain one (or the reverse) on the same path calls `Set` with the *same* `Kind` at a *different* precedence, which used to keep the stale sorted position. Fixed by making `Set<T>` compare the existing contribution's precedence to the new one: equal precedence still replaces in place (unchanged, last-writer-wins-among-equals); a differing precedence removes the old entry and re-inserts at the position `FindInsertionIndex` gives the new one. One fix covers both P1s (entering the override from a plain state, and leaving it for one), since both are the same in-place-replacement bug read in each direction.
  A second, discovered-while-reproducing defect shared the same root symptom but a different location, outside this slice's original perimeter (`MGUI.Core/UI/Styling/UIResolvedPropertyStore.cs`, `MGUI.Core/UI/Animation/States/UIVisualState.cs`, `MGUI.Core/UI/XAML/Animation.cs`): every tagged sub-slot writer that gates its *physical* container write — `MGElement.SetBackgroundSlot`, `MGElement.SetBackgroundFocusedColor`, `MGElement.SetDefaultTextForegroundSlot`, `MGTextBlock.SetForegroundSlot` — checked `effective.Source.Kind == source.Kind` ("did MY write become the winner?"), discarding the `effectiveChanged` output `Set<T>` already computes (`out _`). That check cannot detect the one new case U4 introduces: this call's own contribution loses the top slot to a *different*, already-recorded kind as a direct result of the reorder above (e.g. a `VisualState` write at 70 displacing itself below an existing tagged `LocalValue` at 90) — the winner changes, but not to this caller's kind, so the physical field was silently left showing the previous (now-wrong) value. Perimeter extended to these two files for this reason: the store-only fix is logically complete (`EnumerateResolvedContributions`/`TryGetResolvedValueSource` were already correct with it alone, which is what the regression test first caught as a **physical** mismatch, not a logical one) but the user-visible acceptance (the *rendered* colour) stays broken without it — declared here rather than silently widening scope. Fixed by gating all four on `effectiveChanged` instead of the kind comparison; verified safe by inspection (the two conditions coincide in every pre-U4 case: a call's own kind can only stop being, or start being, the winner when *something* about the effective value changed) and by the full suite staying green.
  Regression test added (`VisualStatesTests.LeavingAnOverridingStateForAPlainStateOnTheSamePath_NeverLeavesTheSeventyAboveALocalValue`): a toggle with an overriding `Checked` state and a plain `Hover` state both declaring `Background`, plus a tagged `LocalValue` write underneath — drives both transition directions (`Hover`-shape plain state up to `Checked` override, then back down) and asserts the *physical* `BackgroundBrush.NormalValue`, the reported `UIValueResolutionSource` (kind and precedence), and the sorted content of `EnumerateResolvedContributions`, closing the P4 advisory that no existing U4 test combined two states on one path. Full suite reverified green: 2125/2125 (2124 + 1 new test); `MGUI.Core`/`MGUI.Tests` build clean; `MGUI.Samples` compiles (`-t:Compile`).
- Local value versus state, Fix round 2 (13 September 2026): a second independent verifier REFUTED fix round 1 (one P2) — `effectiveChanged` alone is *not* a strict superset of the pre-U4 `effective.Source.Kind == source.Kind` gate it replaced, contrary to the round 1 note above claiming safety by inspection. The missed case: the current winner rewrites its *own*, value-equal, contribution (a settled animation re-applying its resting value, or a state re-entered with the same setter value) — `effectiveChanged` is then false, so a stale value written in between by an *untagged* facade call to the same sub-slot (which always applies physically per R5, regardless of precedence, and records a dormant contribution under the still-winning kind) is never corrected back to the winner's value. Value-compared sub-slots (`Color?`: `Background.FocusedColor`, `DefaultTextForeground`, `MGTextBlock.Foreground`) hit this readily; brush sub-slots are mostly shielded because their `Set<T>` call passes `ReferenceEqualityComparer`, under which a freshly-constructed brush of the same visual colour still compares as "changed". Fixed by widening all four gates (`MGElement.SetBackgroundSlot`, `SetBackgroundFocusedColor`, `SetDefaultTextForegroundSlot`, `MGTextBlock.SetForegroundSlot`) to `effectiveChanged || effective.Source.Kind == source.Kind`: the OR keeps the round 1 fix (a different kind taking the win still re-syncs the physical field) while restoring the old gate as a fallback (the winner can always re-sync the physical field to its own current value, changed or not). No change was needed to `UIResolvedPropertyStore.Set<T>` — the round 1 sort-order fix there is unaffected; only the four sub-slot gates were incomplete.
  Regression test added (`VisualStatesTests.TheWinnerRewritingItsOwnUnchangedValue_StillResyncsAStaleUntaggedSubField`): `SetBackgroundFocusedColor(Red, Animation source)` (physical = Red) → untagged `toggle.BackgroundBrush.FocusedColor = Blue` (applies physically, records a dormant `LocalValue` at 90 under the `Animation` at 100) → `SetBackgroundFocusedColor(Red, Animation source)` again (the winner rewrites its own unchanged value) → asserts the physical field is Red, not the stale Blue. Confirmed failing (Blue instead of Red) against a temporary revert of `SetBackgroundFocusedColor`'s gate back to `effectiveChanged` alone, and green with the fix restored. The round 1 regression test (`LeavingAnOverridingStateForAPlainStateOnTheSamePath...`) stays green with the OR, since the old gate alone would already have covered that scenario. `MGElement.cs` was found missing its UTF-8 BOM at the start of this round (lost before this round, cause not investigated as out of perimeter) and restored with no other content change. Full suite reverified green: 2126/2126 (2125 + 1 new test); `MGUI.Core`/`MGUI.Tests` build clean.
- Toggle Checked slot (U5, 13 September 2026), drawing hook: `MGElement.DrawBackground`'s two `BackgroundBrush.GetUnderlay(DA.VisualState.Primary)` calls were replaced by one new `protected virtual` hook, `ResolveBackgroundUnderlay(VisualStateFillBrush brush, PrimaryVisualState state)`, whose default body (`brush.GetUnderlay(state)`) is byte-for-byte what both call sites already did — every element other than `MGToggleButton` therefore draws exactly as before this slice, verified by the full suite staying green and by `git diff` on `MGElement.cs` touching only this one addition. `MGToggleButton` overrides it with `brush.GetValue(state, IsChecked)`, which itself degrades to `GetUnderlay(state)` whenever `IsChecked` is false or no `CheckedValue` is set (`GetValue(PrimaryVisualState, bool)`'s own fallback), so the toggle's own unchecked/no-override rendering is unchanged too. The alternative the brief offered — overriding `DrawBackground` wholesale in `MGToggleButton` — was rejected: `DrawBackground` has two call sites (rounded-corner and plain-rectangle paths) plus the overlay draw shared between them, so a full override would either duplicate that logic or partially call back into the base method for the overlay half, both worse than one two-line hook reused by the base method itself. The nine other framework `GetUnderlay` call sites (`MGBorder.cs`, `MGProgressBar.cs`, `MGResizeGrip.cs`, `MGScrollViewer.cs`, `MGWindow.cs`, `UISymbolElements.cs`, plus `MGElement.BackgroundUnderlay`) were left untouched, matching the brief's constraint; grepped after the change to confirm the count and shape did not move.
- Toggle Checked slot (U5, continued), struct-`T` nullable semantics: `VisualStateBrush<T>.CheckedValue`/`HasCheckedValue` needed to express "unset" for both a reference `T` (`IFillBrush`, where `null` is a natural sentinel) and a value `T` (`Color`, used by `VisualStateColorBrush`, where every bit pattern is a legitimate colour). A `Nullable<T>`-shaped wrapper (`CheckedValue` typed `T?`) was rejected because `IFillBrush` is a reference type and generic code cannot apply `Nullable<T>` to an unconstrained `T` without splitting the two brush classes' logic; the setter instead computes `bool isNull = value is null` (a pattern that compiles for any `T`, is always false for a struct, and behaves as an ordinary null check for a reference type) and derives `HasCheckedValue` from it, so `CheckedValue = null` on `VisualStateFillBrush` clears the slot exactly like `ClearCheckedValue()`, while `VisualStateColorBrush.CheckedValue = someColor` always sets `HasCheckedValue = true` (no bit pattern of `Color` can clear it — only `ClearCheckedValue()` can). This mirrors the existing `VisualStateBrush<T>.FocusedColor` field (a genuine `Color?`) only in spirit, not in mechanism: `FocusedColor` could use `Nullable<Color>` directly because it is declared as `Color?` on the class itself, never generic over `T`.
- Toggle Checked slot (U5, continued), theme-change survival — not the mechanism the brief's work item 2 first sketched: an attempt to capture `BackgroundBrush.CheckedValue`/`HasCheckedValue` from the pre-swap container at the top of `MGToggleButton.OnThemeChanged` and copy it onto the post-swap container was tried first and failed a real-theme-change probe (`RealThemeChange_KeepsACodeSetCheckedBackgroundBrush_AndACodeSetCheckedTextForeground`, written before the fix, red against the capture-and-copy attempt) — root cause: `MGElement.NotifyThemeChanged` calls `RefreshThemeBackgroundDefault` (an existing framework step, out of this slice's perimeter, unconditionally run on every theme notification) *before* calling `OnThemeChanged`, and that step can itself swap `BackgroundBrush` for a fresh, checked-value-free clone (`MGTheme.GetBackgroundBrush`'s `ThemeManagedGetter<T>.GetValue(true)` always returns a `Clone()`) whenever the constructor-time `DefaultValue` contribution is still the container's provenance — so by the time `MGToggleButton.OnThemeChanged` runs and takes its "before" snapshot, the checked value could already be gone; confirmed with a temporary diagnostic (`Console.Error.WriteLine` at entry, removed before commit) showing `HasCheckedValue` already `false` on entry to the override, one frame after a real theme swap, while it was still `true` immediately before the swap. Fixed per the brief's second-offered alternative ("keep it on the toggle and re-apply"): both `CheckedBackgroundBrush` and `CheckedTextForeground` are now backed by a private field/flag pair on `MGToggleButton` itself (not solely by the brush instance), and `OnThemeChanged` unconditionally re-applies from that field (`SyncCheckedBackgroundValue`/`SyncCheckedTextForegroundSlot`) after its own `SetBackground` call, regardless of whether either framework step actually swapped the container — verified green against the same probe. `CheckedBackgroundBrush`'s getter/setter therefore reads `BackgroundBrush.SelectedValue` as the fallback (matching the brief's `CheckedValue ?? SelectedValue` phrasing) but the code-set value's source of truth is the toggle field, mirrored onto `BackgroundBrush.CheckedValue` on every write and on every theme change; this does not change `CLAIM`'s externally observable contract (drawing still reads `BackgroundBrush.CheckedValue` through `ResolveBackgroundUnderlay`) or touch `UIValueSlot`/`UIResolvedPropertyStore`/the pilot.
- Toggle Checked slot (U5, continued): `ResolvedPilotWriteSitesTests.cs` needed no change — `BackgroundBrush.CheckedValue = ...` matches neither `PilotAssignmentPattern` (no bare `BackgroundBrush =`) nor `BackgroundSubFieldPattern` (`CheckedValue` is not one of the five enumerated sub-fields), and `SetDefaultTextForegroundSlot(UIValueSlot.Selected, value, source)` is a method call, not an assignment to `DefaultTextForeground`, so it was already invisible to the scan before this slice (as the constructor's and `OnThemeChanged`'s pre-existing calls to it already were). Confirmed by running the file's own tests, not merely by reading the regexes. Tests added: ten in `MGUI.Tests/Animation/ToggleCheckedSlotTests.cs` (listed in the plan's Statut paragraph). Full suite reverified green: 2136/2136; `MGUI.Core`/`MGUI.Tests` build clean; `MGUI.Samples` compiles (`-t:Compile`).
- Toggle Checked slot (U5, fix round 1, 13 September 2026): a fresh verifier found that `SyncCheckedBackgroundValue`'s only two call sites (`CheckedBackgroundBrush`'s setter and `OnThemeChanged`) left every OTHER whole-container replacement of `BackgroundBrush` unguarded — the public `MGElement.BackgroundBrush` setter, `MGExpander.ExpanderButtonBackgroundBrush`'s setter (which assigns the whole `BackgroundBrush` of its public `ExpanderToggleButton`), and a pilot/style Whole-slot resolution all reach `MGElement.SetBackground`/`ApplyBackgroundEffective` without going through the toggle's own setter or `OnThemeChanged`, so a container swap through any of them dropped `CheckedValue` from the fresh container while `CheckedBackgroundBrush`'s getter (backed by the toggle-level field, not the container) kept reporting the old value — the property and the drawn brush disagreed (probe: `ToggleCheckedSlotTests.ReassigningBackgroundBrush_AfterCheckedBrushIsSet_KeepsTheCheckedBrushDrawnAndReported`). Fixed by generalising the re-sync point rather than enumerating callers: `MGElement.ApplyBackgroundEffective` (the single place every whole-container swap already funnels through, `SetBackground`/`ClearBackgroundPilotSource`/`RefreshThemeBackgroundDefault` included) now calls one new `protected virtual` hook, `OnBackgroundBrushContainerReplaced()`, right after `ReapplyBackgroundSubSlots()`; the default body is empty (every element other than `MGToggleButton` is unaffected), and `MGToggleButton` overrides it with `SyncCheckedBackgroundValue()`. This is a second small hook added to `MGElement.cs` beyond U5's original single drawing hook (`ResolveBackgroundUnderlay`); it was judged unavoidable because the alternative — the getter reading `BackgroundBrush.CheckedValue`/`HasCheckedValue` directly instead of the toggle field — would have silently lost the checked brush across a theme change (the exact regression the original slice's theme-change decision, above, was written to prevent), trading one P2 for a re-introduced, previously-fixed bug. The now-redundant explicit `SyncCheckedBackgroundValue()` call in `OnThemeChanged` was removed (theme changes reach the same hook via `RefreshThemeBackgroundDefault`/`SetBackground` → `ApplyBackgroundEffective`, confirmed by rerunning the theme-change probes). `CheckedTextForeground` needed no equivalent fix: it is tracked through `SetDefaultTextForegroundSlot`, a tagged sub-slot pilot write already covered by `ReapplyBackgroundSubSlots`'s existing counterpart for `DefaultTextForeground` on any whole-container swap, unlike `CheckedValue` which is written directly onto the brush instance outside the pilot/resolved-value store entirely (`UIValueSlot` stays unchanged, per the original constraint). Full suite reverified green: 2137/2137; `MGUI.Core`/`MGUI.Tests` build clean; `MGUI.Samples` compiles (`-t:Compile`).
- Toggle Checked slot (U5, fix round 2, 13 September 2026): a fresh verifier REFUTED round 1's claim (above) that `CheckedTextForeground` "needed no equivalent fix" (one P1, one P2). The P1: `SyncCheckedTextForegroundSlot`'s code-set branch wrote the Selected sub-slot with `source` unchanged -- `UIValueResolutionSource.Default` from the property setter/constructor, `UIValueResolutionSource.Theme` from `OnThemeChanged` -- both far below `LocalValue`'s precedence (0 and 20 versus 90). Round 1's "already covered by `ReapplyBackgroundSubSlots`'s counterpart" reasoning addressed only container swaps re-applying the *current pilot winner*; it did not address the winner itself being computed wrong. Once any earlier call (most ordinarily `OnThemeChanged`, itself the only writer of a `Theme`-precedence contribution on this slot for a toggle) had recorded a `Theme`-kind contribution, a later code-set write recorded at `Default` could never outrank it, so `SetDefaultTextForegroundSlot`'s physical re-application kept applying the stale `Theme` value -- `CheckedTextForeground`'s getter (backed by the toggle field, not the resolved slot) still reported the code value, so the property and the resolved/drawn value diverged, the same divergence class round 1 closed for `CheckedBackgroundBrush`. Probe: show toggle -> real theme change -> set `CheckedTextForeground` -> one frame -> `DefaultTextForeground.SelectedValue` stayed the theme colour, not the code colour. Fixed by giving a code-set value real override precedence: the code-set branch now always writes via `UIValueResolutionSource.LocalValue(source.Invalidation)` (ignoring `source`'s own `Kind`/`Precedence`, keeping only its `Invalidation`), so it outranks `Theme` (and `Style`/`Template`/`VisualState`) regardless of write order, matching how every other code-set pilot property on `MGElement` (e.g. the public `DefaultTextForeground` setter itself) already uses `LocalValue` for a direct assignment. The fallback branch (unset) first clears any leftover `LocalValue`-kind contribution via the existing `internal virtual MGElement.ClearPilotSource(UIPilotProperty.DefaultTextForeground, UIValueSlot.Selected, UIValueSourceKind.LocalValue)` (no new method needed -- it is already `internal`, used elsewhere in `MGElement.cs` for the same generic per-property-per-kind clear), then writes the theme fallback colour with the original `source` exactly as before, so unsetting still restores whatever the natural non-code winner is instead of leaving a stale `LocalValue` contribution stuck as the permanent winner. The P2: `SyncCheckedBackgroundValue` (fix round 1's own new call site, invoked unconditionally from `OnBackgroundBrushContainerReplaced` on every whole-`BackgroundBrush` container swap) dereferenced `BackgroundBrush` with no null guard, throwing `NullReferenceException` on `toggle.BackgroundBrush = null` for any toggle, even one that never used `CheckedBackgroundBrush` -- unlike `ReapplyBackgroundSubSlots`, its sibling re-application for the same container swap, which already checks `_backgroundBrush == null` and returns. Fixed with the same one-line guard at the top of `SyncCheckedBackgroundValue`. Regression tests added: `ToggleCheckedSlotTests.RealThemeChange_AfterCheckedTextForegroundIsSetFirst_TheCodeColourStillWins` (the reverse of the existing `RealThemeChange_KeepsACodeSetCheckedBackgroundBrush_AndACodeSetCheckedTextForeground`'s ordering: theme change *then* `CheckedTextForeground` set, the ordering round 1's tests never exercised), asserting both the resolved `DefaultTextForeground.SelectedValue` and the property agree; and `BackgroundBrush_SetToNull_DoesNotThrow_EvenAfterCheckedBackgroundBrushWasUsed`, reproducing the verifier's exact null-assignment probe. Full suite reverified green: 2138/2138 (2137 + 2 new tests); `MGUI.Core`/`MGUI.Tests` build clean; `MGUI.Samples` compiles (`-t:Compile`).
- Keyframe clip (U6, 13 September 2026): `UIKeyFrameSerializer.Format<T>`/`Parse<T>` and its `JsonOptions` field became `internal static` (unchanged behaviour: `Serialize<T>`/`Deserialize<T>`/`ReadValueType` stay `public` and untouched; `KeyFrameTests` green with no edit), plus a new `internal static bool IsSupported(Type)` so `UIKeyFrameClipSerializer` can refuse a registered path whose value type `Format`/`Parse` do not handle (found via `UIAnimationTargets.GetValueType`) before attempting to parse it, rather than only catching a `NotSupportedException` from a generic method invoked through reflection. `UIKeyFrameClipSerializer.Serialize`/`Deserialize` resolve each track's `T` at runtime with `MakeGenericType`/`MakeGenericMethod` (precedent: `UITransition.Create`), the same shape used by U2's `UIAnimationTargets.Entry` and U3's store helpers elsewhere in this program. `UIStoryboard.Duration` is a public property of the `UIAnimation` base and therefore already "exposed" as the brief's work item 19 asked without any change to `UIStoryboard.cs` (the PERIMETER's conditional file): at serialisation, the clip's authoritative duration is `storyboard.Duration` when it is non-zero (the value `UIAnimationGroup.OnStarting`/`ComputeDuration` writes once the storyboard has actually been played), falling back to the longest child's `Duration` for a storyboard built but never started (the ordinary shape for a clip constructed purely to be serialised) -- `ComputeDuration()` itself stays `protected` and was not touched or exposed, since every child is required (a few lines below) to already equal this same value, making the fallback's own computation cheap and side-effect-free. Duplicate-path tracks (two `UIKeyFrameAnimation<T>` on the same `Property`) are deliberately not refused at deserialisation: `UIAnimationManager.Start`'s existing one-animation-per-owner-per-path conflict rule (cancelling the previous registrant with `UIAnimationCancelBehavior.KeepCurrent`) already makes the outcome deterministic once the storyboard starts its children in `Children` order (file order), so the later track always wins -- verified by a dedicated test rather than assumed, and left undocumented as a refusal so a future multi-layer compositing feature is not blocked by an artificial constraint this slice does not need. Reflection errors are unwrapped: `Type.GetMethod(...).MakeGenericMethod(...).Invoke(...)` on `BuildChild<T>` wraps a `FormatException`/`InvalidOperationException` from a malformed frame or an invalid track in a `TargetInvocationException`; `Deserialize` catches it and rethrows `.InnerException` so a caller sees exactly the same exception type and message `BuildChild<T>` raised, matching how a direct (non-reflected) call would have failed. Full suite verified green: 2154/2154 (2139 + 15 new tests, `KeyFrameClipTests.cs`); `MGUI.Samples` compiles (`-t:Compile`).
